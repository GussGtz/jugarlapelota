# Protocolo Domesticado

Bitácora de contexto del proyecto **JugarLaPelota**. Cada vez que se haga un cambio relevante, se registra aquí para tener siempre a mano el historial y las decisiones tomadas.

Repo: `git@github.com:GussGtz/jugarlapelota.git`
Stack: Vue 3 + Vite + Tailwind + Pinia + Socket.io (frontend) / Node.js + Express + MySQL + Cloudinary (backend)

---

## Cómo usar este documento

- Cuando el usuario diga "ejecuta el protocolo domesticado", significa: leer este archivo completo para recuperar el contexto acumulado del proyecto antes de continuar trabajando.
- Cada cambio nuevo (feature, fix, decisión de arquitectura, etc.) se agrega como una entrada nueva al final del log, con fecha, resumen y motivo.

---

## Log de cambios

### 2026-07-02 — Creación del protocolo domesticado
- Se confirma que el repo ya está clonado localmente en `/Users/samuelgutierrez/Desktop/JugarLaPelota`, apuntando a `origin = git@github.com:GussGtz/jugarlapelota.git`.
- Se crea este documento (`PROTOCOLO_DOMESTICADO.md`) como bitácora central de contexto del proyecto.
- Último commit en `main` al momento de crear este documento: `16aa9c5 fix: push notifications end-to-end audit`.

### 2026-07-02 — Fix: "seguir equipo/torneo" ahora persiste en BD ligado a la cuenta
**Problema reportado:** el usuario notó que seguir un equipo o torneo se perdía al iniciar sesión en otro dispositivo o al pasar el tiempo.

**Causa raíz encontrada:** `src/stores/following.js` guardaba el follow en `localStorage` siempre, pero solo lo sincronizaba con el backend (`team_follows` / `tournament_follows`) cuando existía un `endpoint` de suscripción Web Push — es decir, el follow quedaba atado al **dispositivo/navegador**, no a la cuenta (`user_id`). Las tablas `team_follows`/`tournament_follows` ni siquiera tenían columna `user_id`.

**Solución implementada:**
- Backend: nuevas tablas `user_team_follows` y `user_tournament_follows` (PK `user_id + team_id/tournament_id`), agregadas en [backend/config/db-schema.js](backend/config/db-schema.js) (PG_MIGRATIONS) y [backend/config/db-sqlite-init.js](backend/config/db-sqlite-init.js). De paso se corrigió que `tournament_follows` faltaba por completo en el init de SQLite (bug preexistente que rompía el dev local).
- Rutas `/follows`, `/follows/add`, `/follows/remove`, `/follows/tournaments`, `/follows/tournament/add`, `/follows/tournament/remove` en [backend/routes/index.js](backend/routes/index.js) ahora usan `optionalAuth` y leen/escriben en las tablas `user_*_follows` cuando hay `req.user.id`, además del flujo existente por `endpoint` (que se conserva para el targeting de push).
- `sendPushToTeams`/`sendPushToTournaments` ahora también hacen JOIN por `push_subscriptions.user_id`, para que un usuario logueado reciba notificaciones sin importar en qué dispositivo siguió al equipo/torneo.
- Frontend: `src/stores/following.js` ahora consulta `useAuthStore().isLoggedIn` — si hay sesión, llama al backend aunque no haya push activado. `syncFromServer` hace merge (no overwrite) para no perder follows anónimos hechos antes de loguearse.
- `src/main.js` sincroniza follows automáticamente al arrancar si hay sesión válida (`verifySession` → `syncFromServer`). `Login.vue` también sincroniza tras login con Google.

**Validado con:** build de Vite exitoso, y prueba manual vía curl contra SQLite local: follow sin `endpoint` pero con JWT válido se persiste y es legible solo con ese token (no visible sin token), confirmando que ya no depende del dispositivo.

**Nota histórica:** en este cambio no se tocó el requisito de login para seguir un torneo (se mantuvo el flujo anónimo con solo push). Ver siguiente entrada — se corrigió después para exigir login también en torneos.

### 2026-07-02 — Fix: seguir torneo ahora exige login (igual que equipos)
**Problema reportado:** seguir un equipo ya exigía login primero (`TeamDetail.vue`), pero seguir un torneo no — bastaba con aceptar notificaciones push, quedando inconsistente y permitiendo follows anónimos que no viven en la cuenta.

**Solución:** [src/pages/TournamentHome.vue](src/pages/TournamentHome.vue) `toggleFollow()` ahora replica la misma guarda que `TeamDetail.vue`: si `!auth.isLoggedIn`, redirige a `{ name: 'Login', query: { redirect: route.fullPath } }` antes de intentar seguir, en vez de solo pedir permiso de push.

**Validado con:** preview en navegador contra backend local (SQLite) — se creó `.env.local` temporal apuntando a `localhost:3000` (el proyecto usa producción/Railway por defecto en `.env`) y se corrió el backend con `FRONTEND_URL=http://localhost:5175` para pasar CORS. Clic en "SEGUIR TORNEO" sin sesión → redirige correctamente a `/login?redirect=/COPACARIBE`. Se limpió `.env.local` al terminar (no se commitea, ya está en `.gitignore`).

### 2026-07-02 — Feature: tutoriales de instalación de la PWA en el Home
**Contexto:** el usuario notó que al ser una PWA (no está en App Store/Play Store), los usuarios probablemente no saben cómo instalarla en su celular. Compartió dos videos grabados (`instalar android.MOV`, `instalar apple.MOV`) ubicados en `~/Downloads/`.

**Detalle técnico importante:** los .MOV originales venían codificados en **HEVC** (H.265), que Firefox no soporta y muchos navegadores/Android tampoco reproducen de forma confiable. Se transcodificaron a H.264/AAC en contenedor MP4 usando la herramienta nativa de macOS `avconvert` (preset `PresetAppleM4V1080pHD`) — no había `ffmpeg` ni `brew` instalados en el entorno. Quedaron en [public/videos/instalar-android.mp4](public/videos/instalar-android.mp4) y [public/videos/instalar-apple.mp4](public/videos/instalar-apple.mp4) (~9MB y ~8.9MB).

**Nota sobre `.gitignore`:** el repo excluye `*.mp4/*.mov/*.avi` globalmente porque supera el límite de 100MB de GitHub (el video del Hero, `hero-bg.mp4` de 143MB, de hecho ni está en git — se sirve embebido vía Streamable externo). Como estos tutoriales pesan ~9MB c/u, muy por debajo del límite, se le preguntó al usuario cómo prefería publicarlos: commitear directo vs. subir a Cloudinary. Eligió **commitear directo** — se agregaron excepciones puntuales en `.gitignore` (`!public/videos/instalar-android.mp4`, `!public/videos/instalar-apple.mp4`) para esos dos archivos específicos únicamente.

**Solución:** nuevo componente [src/components/InstallTutorial/InstallTutorial.vue](src/components/InstallTutorial/InstallTutorial.vue) — dos tarjetas con thumbnail de video (Android / iPhone), al hacer clic abren un modal con el video en reproducción con controles nativos. Integrado en [src/pages/Home.vue](src/pages/Home.vue) entre la sección de torneos activos y la sección "Un torneo conectado para todos".

**Validado con:** build de Vite exitoso (confirmado que Vite copia `public/videos/` a `dist/videos/` automáticamente), y prueba en navegador de preview — clic en la tarjeta "Android" abre el modal y el video reproduce correctamente, sin errores de consola.

### 2026-07-02 — Style: tutorial + características en dos columnas
**Pedido:** poner el tutorial de instalación y la sección "Un torneo conectado para todos" en la misma línea (tutorial a la izquierda, características a la derecha), y cambiar el copy introductorio del tutorial.

**Solución:** se fusionaron ambas secciones en un solo `<section>` con `grid lg:grid-cols-2` en [src/pages/Home.vue](src/pages/Home.vue) — en desktop quedan lado a lado, en mobile se apilan (tutorial arriba). [InstallTutorial.vue](src/components/InstallTutorial/InstallTutorial.vue) se refactorizó de `<section>` centrada a un `<div>` alineado a la izquierda para encajar como columna. Texto cambiado a: "Sin descargas pesadas ni tiendas de apps. Más rápido, más ligero y sin ocupar espacio en tu memoria."

**Validado con:** preview en navegador en desktop (1280px, columnas lado a lado) y mobile (375px, apilado), sin errores de consola.

### 2026-07-02 — Fix: centrado dentro de columnas + videos invisibles en móvil
**Problema reportado:** (1) el contenido de ambas columnas quedaba "esquinado" (pegado a los bordes exteriores) en vez de centrado dentro de su propio espacio; (2) en modo móvil los videos tutoriales no se veían en absoluto.

**Causa raíz de (2):** los `<video>` miniatura no tenían `poster`, solo `preload="metadata"`. Muchos navegadores móviles (Safari iOS, Chrome Android) no pintan ningún frame del video hasta que el usuario interactúa con él — el resultado es un recuadro negro/vacío hasta el primer play. En desktop (Chrome) sí se ve un frame porque ese navegador sí decodifica el primer fotograma solo con los metadatos.

**Solución:**
- Se generaron imágenes poster estáticas (`.jpg`, ~54KB c/u) con `qlmanage -t` (thumbnail nativo de macOS) a partir de los `.mp4`, convertidas con `sips`. Quedaron en [public/videos/instalar-android-poster.jpg](public/videos/instalar-android-poster.jpg) y [public/videos/instalar-apple-poster.jpg](public/videos/instalar-apple-poster.jpg), referenciadas vía el atributo `poster` en [InstallTutorial.vue](src/components/InstallTutorial/InstallTutorial.vue). Esto garantiza que el thumbnail se vea en cualquier navegador sin depender de que decodifique el video.
- Se cambió `preload="metadata"` a `preload="none"` en los videos miniatura (ya no hace falta cargar nada del video hasta que el usuario haga clic, porque el poster cubre esa función).
- Centrado: se agregó `text-center` + `mx-auto` en los elementos internos de cada columna (encabezado, párrafo con `max-w-sm mx-auto`, grid de videos/cards con `max-w-sm`/`max-w-md mx-auto`) tanto en `InstallTutorial.vue` como en la columna de "características" en [Home.vue](src/pages/Home.vue), en vez de dejarlos pegados al borde izquierdo/derecho de cada columna.

**Validado con:** preview en navegador en desktop (1280px), tablet (768px) y mobile (375px) — contenido centrado dentro de cada columna en las tres resoluciones, videos con poster visible incluso sin reproducir, sin errores de consola.

### 2026-07-02 — Fix: botón Hero "Instalar app" + modal de video no invasivo en móvil
**Pedido:** (1) cambiar el botón "Comenzar" del Hero (iba a `/login`) por "Instalar app" que haga scroll hasta el tutorial; (2) que el modal de video no ocupe toda la pantalla en móvil (dejar que el usuario lo amplíe con el botón nativo de fullscreen si quiere); (3) la X de cerrar no se podía usar en móvil.

**Causa de (3):** la X estaba posicionada `absolute -top-11` (44px por encima del contenedor del video, fuera de él). Con el video sin acotar en altura (ocupaba casi toda la pantalla en móvil por el aspect-ratio portrait), ese offset quedaba fuera del viewport visible.

**Solución:**
- [HeroSection.vue](src/components/Hero/HeroSection.vue): el botón ahora es `@click="scrollToInstall"` → `document.getElementById('instalar-app')?.scrollIntoView(...)`. Se agregó `id="instalar-app"` (+ `scroll-mt-20` para no quedar tapado por el navbar fijo) al contenedor raíz de [InstallTutorial.vue](src/components/InstallTutorial/InstallTutorial.vue).
- El video del modal ahora usa `max-h-[65vh] max-w-[80vw] sm:max-w-xs w-auto h-auto` en vez de `w-full` — queda acotado y se nota que es un overlay, no pantalla completa. El botón nativo de fullscreen de los controles del `<video>` sigue disponible para quien sí quiera ampliarlo.
- La X de cerrar se movió a `absolute top-2 right-2` **sobre la esquina del propio video** (contenedor `inline-block` que se ajusta al tamaño real del video), en vez de flotar por encima fuera de su caja — así siempre queda visible y alcanzable sin importar el alto del video.

**Validado con:** preview en navegador mobile (375px) — clic en "Instalar app" hace scroll correcto hasta el tutorial; clic en thumbnail abre modal compacto (no fullscreen) con la X visible en la esquina; clic en la X cierra correctamente. Sin errores de consola.

### 2026-07-02 — Fix: los cambios de cada deploy no llegaban a las PWAs ya instaladas
**Problema reportado:** al hacer push/deploy, quienes ya tenían la app instalada (PWA) no veían los cambios nuevos.

**Causa raíz:** `vite-plugin-pwa` con `injectRegister: 'auto'` genera un `registerSW.js` que solo hace `navigator.serviceWorker.register(...)` una vez al cargar la página, sin ningún chequeo periódico de actualización. El navegador revisa por su cuenta si hay un Service Worker nuevo solo en navegaciones completas o cada ~24h — pero una PWA instalada que se reabre desde el ícono del home no siempre dispara eso. Aunque `registerType: 'autoUpdate'` hace que el SW nuevo se auto-active (`skipWaiting` + `clientsClaim`) en cuanto se detecta, si nunca se detecta pronto, el usuario sigue en la versión vieja indefinidamente.

**Solución:**
- [vite.config.js](vite.config.js): `injectRegister: false` (se apaga el registro automático por defecto).
- Nuevo [src/pwa.js](src/pwa.js) — registro manual vía `virtual:pwa-register`, que:
  - Revisa actualizaciones (`registration.update()`) cada vez que la app vuelve a primer plano (`visibilitychange`) — cubre el caso real de "reabrir la PWA desde el home" — y cada 30 min mientras sigue abierta en sesiones largas.
  - Al detectar que un Service Worker nuevo tomó control (`controllerchange`), recarga la página automáticamente para que el build nuevo se use de inmediato, en vez de esperar a que el usuario cierre y reabra la app manualmente. Se ignora el primer `controllerchange` (activación inicial en visita nueva, no un update real) para no forzar un reload innecesario a usuarios nuevos.
- Se llama `setupPWAUpdates()` desde [main.js](src/main.js) al arrancar.

**Nota:** para probar esto localmente hace falta el build real (`npm run build` + `vite preview`), porque el plugin de PWA no genera Service Worker en modo dev. Se agregó una config `jlp-frontend-dist` en `.claude/launch.json` (no se commitea, está en `.gitignore`) para levantar `vite preview` en el puerto 5176 cuando se necesite probar esto de nuevo.

**Validado con:** build de producción + `vite preview` — el Service Worker se registra, `registration.update()` no lanza errores, `navigator.serviceWorker.controller` queda activo. Sin errores de consola.

### 2026-07-02 — Feature: portada de patrocinadores en la pestaña Media
**Pedido:** en la pestaña "Media" del torneo público, agregar una foto de portada fija arriba del contenido para mostrar patrocinadores, cargable desde el admin.

**Contexto encontrado:** ya existían en el schema una tabla `sponsors` (por patrocinador individual: nombre/logo/url) y una tabla `banners` (genérica), pero **ninguna de las dos tenía rutas backend ni UI de admin implementadas** — quedaron como schema muerto. Dado que el pedido es una sola imagen fija (no un CRUD de patrocinadores individuales), se optó por el camino más simple y consistente con el patrón ya usado para `logo`/`banner` del torneo: una columna nueva.

**Solución:**
- Columna `sponsors_banner TEXT` en `tournaments` — [backend/config/db-schema.js](backend/config/db-schema.js) (PG_MIGRATIONS) y [backend/config/db-sqlite-init.js](backend/config/db-sqlite-init.js).
- [backend/controllers/tournaments.controller.js](backend/controllers/tournaments.controller.js) `create`/`update` — acepta `sponsorsBanner` en el body (camelCase, mismo patrón que `primaryColor`/`startDate`), lo persiste en `sponsors_banner`. El `getBySlug`/`getAll` ya usan `SELECT *`, así que no necesitaron cambios.
- [src/pages/admin/Tournaments.vue](src/pages/admin/Tournaments.vue) — nuevo `<ImageUpload v-model="form.sponsorsBanner">` en la sección "Imágenes" del form de torneo (mismo componente que ya usan logo/banner, sube a Cloudinary).
- [src/pages/Media.vue](src/pages/Media.vue) — si `tournament.sponsors_banner` existe, se renderiza como imagen fija arriba del feed (antes de streams en vivo y noticias/fotos).

**Nota de bug encontrado (no relacionado, no corregido):** al probar esto localmente contra SQLite se detectó que `GET /tournaments/:slug/players/phase-stats` (backend/routes/index.js:565) lanza 500 (`unrecognized token: ":"`) — sintaxis SQL específica de Postgres que no es compatible con SQLite. Solo afecta al dev local (producción usa Postgres). Se dejó un task flag (`task_428a9abc`) para corregirlo en otra sesión.

**Validado con:** flujo completo end-to-end contra backend local (SQLite) — `PUT /tournaments/:id` con `sponsorsBanner` persiste y se lee correctamente vía API; en el navegador, la imagen se ve fija arriba del feed en `/COPACARIBE/media`; el campo aparece correctamente en el modal de edición del admin con su imagen y texto de ayuda. Sin errores de consola relacionados a este cambio.

### 2026-07-02 — Fix: `/players/phase-stats` roto en SQLite (bug del incidente anterior)
**Causa raíz:** [backend/routes/index.js:601](backend/routes/index.js:601) usaba `m.phase_id = ANY($1::bigint[])` — sintaxis de arrays exclusiva de Postgres (cast `::bigint[]` + `ANY()`). SQLite no la entiende en absoluto, de ahí el error `unrecognized token: ":"` al correr el backend local en modo SQLite (sin `DATABASE_URL`). En producción (Postgres) nunca se notaba.

**Solución:** se reemplazó por `IN (${placeholders dinámicos})` — mismo patrón portable ya usado en `sendPushToTeams`/`sendPushToTournaments` (ver entrada del 2026-07-02 sobre follows). Los placeholders del filtro de categoría (`catFilter`) se recalculan según la nueva cantidad de params.

**Validado con:** backend local (SQLite) — `GET /tournaments/COPACARIBE/players/phase-stats` (con y sin `?cat=`) responde 200 con datos correctos en vez de 500. Páginas admin Dashboard y Jugadores cargan sin errores de consola.

### 2026-07-02 — Revisión completa del proyecto sin errores
**Pedido:** revisar que todo el proyecto funcione sin errores.

**Metodología:**
1. `node -c` en todos los `.js` de `backend/` — sin errores de sintaxis.
2. `npm run build` (frontend) — build limpio, sin warnings.
3. Comparación sistemática de tablas `CREATE TABLE` entre `backend/config/db-schema.js` (Postgres) y `backend/config/db-sqlite-init.js` — reveló que **faltaba `inscription_responsables`** en SQLite (mismo patrón que el bug de `tournament_follows` de una sesión anterior). Se agregó la tabla siguiendo el mismo estilo que `inscription_players` (tabla vecina).
4. Smoke test con `curl` de ~20 endpoints backend (públicos y con auth admin: tournaments, categories, matches, news, galleries, streams, players/phase-stats, awards, teams, follows, inscriptions, responsables, phases) — todos 200 tras el fix.
5. Recorrido en navegador (preview local contra backend SQLite) de: Home, Torneo (Inicio/Partidos/Media), TeamDetail, Players, Standings, Login, y Admin completo (Dashboard, Torneos, Equipos, Jugadores, Inscripciones, Galería, Noticias, Transmisiones, Premios, Categorías, Partidos) — **sin errores de consola en ninguna**.

**Bug encontrado y corregido:** `GET /tournaments/:slug/responsables` (backend/routes/index.js:1904) daba 500 (`no such table: inscription_responsables`) en SQLite local — tabla presente en el schema de Postgres pero nunca agregada al init de SQLite. Corregido en [backend/config/db-sqlite-init.js](backend/config/db-sqlite-init.js).

**Conclusión:** con este fix, la paridad de tablas entre Postgres (producción) y SQLite (dev local) queda completa — las dos listas de `CREATE TABLE` coinciden exactamente. Build de frontend limpio, backend sin errores de sintaxis, y recorrido completo de páginas públicas + admin sin errores de consola.

### 2026-07-02 — Feature: animaciones y micro-interacciones en el Home público
**Pedido:** hacer más atractiva/profesional (con buena UX y diseño) la sección de "Instala la app" + "Un torneo conectado para todos", y en general todo el lado público/aficionado del sitio.

**Solución:** nueva directiva global reutilizable `v-reveal` ([src/directives/reveal.js](src/directives/reveal.js)) — fade + slide-up cuando el elemento entra al viewport, con un solo `IntersectionObserver` compartido para toda la app (más eficiente que instanciar uno por componente). Soporta `v-reveal="{ delay: N }"` para escalonar listas. CSS base en [src/styles/main.css](src/styles/main.css) con fallback a `prefers-reduced-motion`. Registrada globalmente en [src/main.js](src/main.js).

Nota: `FeatureSections.vue` ya tenía su propio sistema de reveal con `IntersectionObserver` manual por componente (patrón preexistente) — no se tocó esa lógica, solo se le agregó hover premium a los mockups de teléfono (`translateY` + shadow al hover). El resto de la home usa la nueva directiva.

**Aplicado en:**
- [HeroSection.vue](src/components/Hero/HeroSection.vue) — como es contenido visible desde el primer momento (above the fold), no usa scroll-reveal sino una entrada escalonada al montar (`hero-anim` + `animation-delay` por elemento: badge, headline, features, CTAs, stat cards). Badge "Marcadores en tiempo real" con flotado continuo sutil.
- [InstallTutorial.vue](src/components/InstallTutorial/InstallTutorial.vue) — reveal escalonado en badge/heading/párrafo/videos; hover premium en las tarjetas (scale + shadow + zoom del video); anillo de pulso alrededor del botón de play para dirigir la atención; modal con transición fade+scale (antes solo fade).
- [Home.vue](src/pages/Home.vue) — stagger en grid de torneos activos (hover con zoom de imagen + lift) y en las 4 role-cards de "Un torneo conectado para todos" (con micro-rotación del ícono al hover).
- [ContratarBanner.vue](src/components/ContratarBanner/ContratarBanner.vue) — reveal en texto/CTA, círculos decorativos de fondo con drift continuo.

**Validado con:** build de Vite exitoso, preview en desktop (1280px) y mobile (375px) — animaciones se disparan correctamente al hacer scroll, hover states funcionan, el modal de video sigue abriendo/cerrando bien con la nueva transición, sin errores de consola en ninguna resolución.

### 2026-07-02 — Feature: animaciones en la página pública de cada torneo
**Pedido:** extender las animaciones también al "apartado que ve el user de cada torneo" — es decir, [TournamentHome.vue](src/pages/TournamentHome.vue), la página pública que ve un aficionado al entrar a un torneo específico (no solo el Home general del sitio).

**Alcance:** TournamentHome.vue tiene 7 secciones grandes (Hero, partidos en vivo/resultados/próximos, dinámica de grupos-eliminatorias-campeones, noticias, galería, equipos participantes, reconocimientos). Se reutilizó la misma directiva `v-reveal` creada en la entrada anterior para todas, más una entrada escalonada tipo "hero-anim" (clase local `th-hero-anim`, mismo patrón que `hero-anim` de `HeroSection.vue`) para el Hero del torneo, ya que es contenido visible desde el primer momento (above the fold) y no debe depender de scroll.

**Detalle técnico importante:** al combinar `.th-hero-anim` (que define `animation: thHeroFadeUp ...`) con la clase `animate-bounce` de Tailwind (que define `animation: bounce ...`) en el mismo elemento, solo una de las dos reglas `animation` gana en cascada CSS — no se combinan automáticamente. Se solucionó separando en dos elementos anidados: el contenedor externo con `th-hero-anim` (fade+slide de entrada) y un `<div>` interno con `animate-bounce` (rebote continuo del ícono de scroll).

**Stagger acotado con módulo:** en grids largos (equipos participantes, reconocimientos con muchas tarjetas) se usó `(i % N) * delay` en vez de `i * delay` sin acotar, para que el último elemento de una lista de 50 no tarde varios segundos en aparecer.

**Validado con:** build de Vite exitoso, recorrido completo en preview desktop (1280px) y mobile (375px) haciendo scroll por las 7 secciones del torneo COPACARIBE (que está en modo "finalizado" con podio de campeones) — todas las animaciones se disparan correctamente, sin errores de consola.

### 2026-07-06 — Torneo demo "Copa Caribe Demo" en producción + 3 bugs de Postgres corregidos
**Pedido:** crear en producción (mismo perfil admin, torneo separado del "Copa Caribe" real) un torneo de demostración con 7 categorías (las 6 del torneo real + Sub-17 extra), cada una en un estado distinto para mostrar toda la funcionalidad del proyecto en una demo en vivo: 2 finalizadas con campeón/goleador/mejor portero, una a la mitad de fase de grupos, dos con la eliminatoria en distintos puntos de avance, y una reservada "en cero" (solo equipos/jugadores registrados) para que el usuario genere las fases y arbitre un partido en vivo durante la demostración.

**Cómo se hizo:** script Node (`/tmp/.../scratchpad/seed-demo.js`, no versionado — es una herramienta de un solo uso) que llama directo a los endpoints admin de la API (login, `/tournaments`, `/categories` + PUT límites de edad, `/teams`, `/players`, `/tournaments/:slug/auto-setup`, `/phases/:id/groups/generate`, `/matches/:id/start`+`/events`+`/status` para partidos con estadísticas reales, `/phases/:id/advance-to-knockout`). Probado primero exhaustivamente contra el backend local (SQLite) antes de tocar producción.

**3 bugs reales de producción encontrados y corregidos** (todos con el mismo patrón: SQLite es permisivo, Postgres exige SQL estricto — por eso nunca aparecían en desarrollo local):
1. `autoGenerateAwardsForPhase` — `SELECT t.id AS team_id ... GROUP BY p.id` (columna no agregada fuera del GROUP BY). Rompía con 500 real cualquier finalización de fase con goles registrados, en cualquier torneo de producción. Fix: agregar `t.id` al GROUP BY.
2. Mismo `autoGenerateAwardsForPhase`, cálculo de "mejor portero" — `HAVING played > 0` referenciaba un alias del SELECT, inválido en Postgres (SQLite sí lo permite). Fix: `HAVING COUNT(m.id) > 0`.
3. `autoGenerateKnockoutBracket` — calculaba standings de grupo con `team_id` como si fuera columna de `matches` (no existe); estaba silenciado por un try/catch envolvente así que nunca se veía como error, pero significaba que el bracket automático **nunca se generaba** por esa vía en ningún torneo real. Reemplazado por `getGroupStandings()`, la misma función que ya usa `/phases/:id/advance-to-knockout` correctamente. También se corrigió un GROUP BY incompleto análogo en `/admin/awards/scan-all`.

Como también se encontraron ~20 columnas faltantes en el init de SQLite local (ver entrada de "Revisión completa del proyecto"), ya quedaron agregadas antes de esta sesión — sin eso, ni siquiera se hubiera podido probar localmente el flujo de categorías con límites de edad.

**Otros 2 bugs (de mi script de seed, no del producto) encontrados y corregidos durante las pruebas:**
- Nombres de jugador generados únicos por equipo en vez de por categoría completa → colisión 409 (el backend correctamente rechaza nombres duplicados dentro de la misma categoría).
- Marcadores aleatorios de partidos de eliminación podían empatar → el sistema correctamente no avanza el bracket en un empate ("avance manual requerido"), así que se generó un `randScoreNoTie()` dedicado para semifinales/final/tercer lugar.

**Resultado final verificado contra la API real:** 70 equipos (10 × 7 categorías), 700 jugadores, awards de campeón/goleador/portero en Sub-5 y Sub-7, Sub-9 con grupos a la mitad, Sub-11 con la Final pendiente sin jugar, Sub-13 y Sub-15 con eliminatoria generada y semifinales sin jugar (Sub-15 es la reservada para la demo en vivo), Sub-17 con equipos y jugadores registrados pero **cero fases/partidos** (para que el admin genere todo en vivo).

### 2026-07-10 — Feature: visor de documento del jugador en modal (admin)
**Pedido:** en "Jugadores y Responsables" (admin), el botón "Ver" de la columna Doc. abría/descargaba el documento oficial del jugador dejando que el navegador decidiera qué hacer. Se pidió que en su lugar abra en una pestaña/modal responsivo dentro de la app, que muestre cualquier tipo de archivo, cerrable con una X.

**Solución:** nuevo componente reutilizable [DocumentViewerModal.vue](src/components/DocumentViewer/DocumentViewerModal.vue) — reutiliza el sistema de modales ya existente del admin (`.modal-overlay`/`.modal-sheet-lg`/`.modal-header`/`.modal-body`, bottom-sheet en mobile / centrado en desktop). Detecta el tipo de archivo por extensión: `.pdf` → `<iframe>`, cualquier otra cosa (imágenes) → `<img>` con `object-contain`. En [Players.vue](src/pages/admin/Players.vue) se reemplazaron los dos `<a target="_blank">` (tabla desktop y cards mobile) por botones que abren el modal con `viewingDoc = p`.

**Campo relevante:** `documento_oficial` acepta `image/*,application/pdf` según el input de carga en `InscriptionPlayers.vue` — de ahí la necesidad de soportar ambos tipos en el visor.

**Validado con:** preview en navegador contra backend local — probado con una imagen real (Cloudinary demo) y un PDF real (Cloudinary demo, `sample.pdf`). Un primer intento con un PDF de prueba de w3.org falló por `X-Frame-Options` (ese dominio bloquea el embebido en iframe en cualquier sitio) — no era un bug del componente, solo mala elección de URL de prueba; con un PDF real de Cloudinary (que no envía esos headers) el iframe cargó correctamente. Modal abre y cierra bien con la X en ambos casos, sin errores de consola.

### 2026-07-10 — Fix real: el documento seguía descargándose en producción (Cloudinary + proxy propio)
**Reporte del usuario:** tras el fix anterior (modal en vez de descarga), en producción real el documento **seguía descargándose**. Investigación en 3 capas:

1. **Causa raíz #1** — `uploadToCloudinary` subía los PDF con `resource_type: 'raw'`. Cloudinary sirve **todos** los recursos `raw` con `Content-Disposition: attachment` sin excepción — el problema estaba en la respuesta HTTP del archivo mismo, no en cómo el frontend lo embebía. Fix inicial: cambiar a `resource_type: 'image'` (Cloudinary sí permite PDFs inline por esa vía).
2. **Causa raíz #2 (bloqueante)** — al probar, la nueva URL `/image/upload/...pdf` daba **401 "deny or ACL failure"**. Diagnosticado con un endpoint temporal (`GET /_debug/cloudinary`, ya removido) que consultó el Admin API de Cloudinary: el recurso en sí era público y sin restricciones (metadata normal), así que el bloqueo era 100% de la cuenta/plan de Cloudinary. Se probó activar "Allow delivery of PDF and ZIP files" en Settings → Security (confirmado guardado por el usuario) y **también URLs firmadas criptográficamente** (`sign_url:true`, que normalmente evaden restricciones de tipo de medio) — ambas siguieron dando 401. Conclusión: es una restricción de plan que no se puede evadir por configuración ni firma.
3. **Solución final (no depende de Cloudinary):** los PDF vuelven a subirse como `raw` (simple, sin restricciones de *entrega* — el bloqueo de Cloudinary solo aplica cuando el navegador del usuario pide el archivo directo; nuestro propio backend SÍ puede descargar los bytes de un recurso `raw` sin problema). Nuevo endpoint `GET /documents/proxy?url=...` que descarga el archivo servidor-a-servidor y lo reenvía con `Content-Disposition: inline` propio — restringido a URLs de `res.cloudinary.com` para evitar SSRF. `DocumentViewerModal.vue` enruta el `<iframe>` por este proxy en vez de la URL directa.
4. **Bug adicional encontrado en el camino:** las URLs `raw` de Cloudinary **no conservan extensión** (a diferencia de `image`), así que detectar "es PDF" por `.pdf` en la URL fallaba siempre para documentos reales de esta app. Corregido en dos frentes: el proxy detecta PDF leyendo la firma real de bytes (`%PDF-`, primeros 5 bytes del archivo) en vez de la URL o el content-type de Cloudinary (que además siempre reporta `octet-stream` para `raw`); el frontend usa el segmento `/raw/upload/` vs `/image/upload/` de la URL (confiable porque viene del `resource_type` real usado al subir) para decidir `<img>` vs `<iframe>+proxy`.

**Validado con:** `curl -I` contra producción real sobre el documento **ya existente** de un jugador real (Daniel Ponce, el mismo que el usuario probó) — confirmado `Content-Type: application/pdf` y `Content-Disposition: inline` (antes: `application/octet-stream` + `attachment`). Confirmado que el proxy rechaza URLs que no sean de `res.cloudinary.com` (protección SSRF, `400 URL inválida`). No se pudo probar en el preview de navegador local por CORS (producción solo permite el origen del frontend real desplegado, no `localhost`) — verificación server-to-server con curl es equivalente y concluyente.

**Nota:** el documento ya existente de Daniel Ponce nunca necesitó volverse a subir — como sigue siendo un recurso `raw` válido en Cloudinary, el proxy lo sirve bien tal cual con la URL que ya tenía guardada.

### 2026-07-10 — Fix: PDF no se veía en móvil (Safari iOS) — renderizado propio con PDF.js
**Reporte del usuario:** con el proxy ya funcionando en PC, en modo móvil (Safari iOS) el documento se mostraba como un ícono de plugin roto en blanco en vez del PDF. Pidió también asegurar que funcione con cualquier tipo de archivo y que, si es PDF, se vean las páginas.

**Causa:** el `<iframe>` apuntando al PDF depende del visor nativo de PDF del navegador para renderizarlo dentro del iframe. Chrome/desktop tiene un plugin de PDF robusto que funciona bien embebido; Safari iOS **no** — es una limitación conocida de WebKit, no arreglable con headers ni con el proxy.

**Solución:** se reemplazó el `<iframe>` por un renderizado propio con `pdfjs-dist` (misma librería que usa Firefox internamente para su visor de PDF) — cada página del PDF se dibuja en su propio `<canvas>` vía `pdf.getPage(i)` → `page.render({ canvasContext, viewport })`. Al ser renderizado a canvas (no depender de ningún plugin nativo del navegador), funciona igual en cualquier motor/navegador, incluido Safari iOS. Se agregó estado de carga ("Cargando documento…") y de error (con link de respaldo "Abrir en una pestaña nueva" apuntando al proxy, por si el PDF viene corrupto).

**Carga perezosa (lazy-load):** `pdfjs-dist` pesa ~500KB — importarlo de forma estática hubiera inflado el bundle de la página de Jugadores (admin) para TODOS los visitantes aunque nunca abrieran un documento. Se cargó con `import()` dinámico dentro de una función `getPdfjs()`, invocada solo al momento de renderizar un PDF real. Confirmado en el build de Vite: el chunk de Players.vue se mantuvo en ~23KB y `pdfjs-dist` quedó en su propio chunk separado (`pdf-*.js`, ~477KB) que solo se descarga cuando se abre un documento PDF.

**Validado con:** build de producción limpio (code-splitting correcto verificado en el log de Vite). Como el proxy de producción no es accesible desde el navegador de preview local (sandbox sin salida de red hacia Railway), se validó la integración real de PDF.js con un script Node aparte que descargó el documento real de producción (Daniel Ponce) a través del proxy y lo parseó con `pdfjs-dist/legacy/build/pdf.mjs`: `status 200`, `content-type: application/pdf`, `numPages: 2`, viewport de página 1 con dimensiones válidas (611×841) — confirma que la librería procesa correctamente los bytes reales que sirve el proxy. La verificación visual en un dispositivo Safari iOS real queda pendiente de que el usuario la confirme tras el despliegue, ya que no hay forma de controlar un Safari iOS real desde este entorno.

### 2026-07-11 — Fix: no se podía editar el documento/CURP de un jugador ya registrado + limpieza al eliminar
**Reporte del usuario:** en "Jugadores y Responsables" (admin), un jugador guardado sin documento quedaba así para siempre — el formulario de edición del admin no tenía campos de CURP ni documento oficial (solo el de alta pública los tenía). Además, al eliminar un jugador sin documento para volver a registrarlo, el sistema lo seguía marcando como "ya registrado" pese a haberlo borrado.

**Investigación (agente en background) reveló 3 causas distintas:**
1. El formulario de "Editar jugador" en [Players.vue](src/pages/admin/Players.vue) mecánicamente sí llama a `PUT /players/:id`, pero nunca tuvo campos de `curp` ni `documento_oficial` — ni en el HTML del modal, ni en el objeto `form`, ni en el `Object.assign` que precarga los datos al abrir "Editar". Por eso no había forma de agregar el documento después del alta inicial.
2. El backend confirmaba el problema: `POST /players` y `PUT /players/:id` en [backend/routes/index.js](backend/routes/index.js) nunca aceptaban `curp` ni `documento_oficial` en el body, así que aunque el frontend los hubiera mandado, se habrían ignorado.
3. **Causa real del "ya registrado" tras borrar:** el registro público de jugadores (`POST /inscriptions/:id/players`) guarda cada jugador en dos tablas — `players` (el roster real/editable) e `inscription_players` (un registro histórico de la solicitud de inscripción, sin relación FK entre ambas). El botón "Eliminar" del admin (`DELETE /players/:id`) solo borraba de `players`, dejando la fila de `inscription_players` huérfana para siempre. Esa fila huérfana es justo lo que consultan los checks de duplicado por CURP del registro público (`inscription_players` líneas ~1791 y ~1805 de `backend/routes/index.js`), así que cualquier reintento de registrar la misma CURP (desde el link público, aunque no desde el propio formulario del admin) seguía siendo rechazado como "ya registrada" pese a que el jugador ya no existía en el roster.

**Solución:**
- Se agregaron los campos **CURP** (con la misma validación de formato/edad que usa el formulario público — regex + rango de mes/día) y **Documento oficial** (imagen o PDF, mismo endpoint `POST /upload` que ya usa la foto del jugador) al modal de alta/edición en `Players.vue` — visibles y editables tanto al crear como al editar un jugador.
- `POST /players` y `PUT /players/:id` ahora aceptan y guardan `curp` (normalizado a mayúsculas) y `documento_oficial`.
- `DELETE /players/:id` ahora, además de borrar la fila de `players`, borra también la fila de `inscription_players` que coincide por CURP + `inscription_id` del equipo (`teams.inscription_id`) — así un jugador eliminado por el admin queda completamente libre para re-registrarse, sea desde el admin o desde el link público de inscripción.

**Validado con:** backend levantado localmente contra SQLite (con `.claude/launch.json` ahora incluye una config `jlp-backend`) apuntando el frontend local a él vía `.env.local` temporal. En el navegador: edité a un jugador real ("Gustavo Gutierrez", sin documento) y le agregué una CURP — el campo mostró el check verde de validación, guardó sin error, y `GET /tournaments/:slug/players` confirmó `curp: "GUTG150101HDFXXX05"` persistido. Para el bug de "ya registrado" tras eliminar: como el POST público de inscripciones tiene un bug propio no relacionado (`ANY($1::bigint[])`, sintaxis de array de Postgres que no se traduce para SQLite — solo rompe en desarrollo local, no en producción) que impidió generar el escenario end-to-end vía la API pública, se insertaron directamente en la base local (vía `better-sqlite3`) una inscripción + equipo + fila de `inscription_players` + jugador que replican exactamente lo que deja el flujo público real. Se confirmó la consulta de duplicado encontraba la fila **antes** de borrar, se llamó al endpoint real `DELETE /players/:id`, y se confirmó que tanto `players` como `inscription_players` quedaron vacíos y la consulta de duplicado ya no encontraba nada **después** — el fix funciona contra el endpoint real, no solo en teoría. No se pudo subir un documento real de prueba en local porque las credenciales de Cloudinary en `backend/.env` son placeholders (`tu_cloud_name`), pero el upload reutiliza el mismo endpoint `/upload` que ya funciona en producción para la foto del jugador, sin tocar `uploadToCloudinary`.

**Nota:** el bug de `ANY($1::bigint[])` en `POST /inscriptions` (backend/routes/index.js:1584) quedó identificado pero sin corregir por estar fuera del alcance de este pedido — solo afecta pruebas locales en SQLite, no producción (que corre en Postgres). Queda pendiente si se quiere poder probar el flujo de inscripciones completo en local alguna vez.

### 2026-07-12 — Fix real: el registro público seguía rechazando CURPs de jugadores ya eliminados + no se podía editar desde el link del delegado
**Reporte del usuario (con captura):** tras el fix anterior, probó en producción real: eliminó un jugador desde el admin y el delegado (desde el link público que se le compartió) intentó re-registrar esa misma CURP — seguía saliendo "ya está registrada en esta categoría". Además, esa vista pública tampoco tenía forma de editar un jugador ya enviado (solo mostraba "Ver doc"/"Sin doc", de solo lectura).

**Causa raíz real (más profunda que el fix anterior):** el fix de la sesión pasada (limpiar `inscription_players` al borrar desde el admin) solo previene huérfanos **futuros**. Pero el verdadero problema de diseño es que las validaciones de CURP duplicada en `POST /inscriptions/:id/players` (líneas ~1799-1832) comparaban contra la tabla **`inscription_players`** (el historial de solicitudes), no contra el roster real (`players`). Cualquier fila vieja en `inscription_players` — de antes de que existiera el fix de limpieza, o de cualquier otro origen — sigue ahí para siempre y sigue bloqueando, sin importar si el jugador real ya no existe.

**Solución (más robusta, autocorrectiva — "self-healing"):** las 3 validaciones de duplicado (CURP misma categoría, CURP cachirul en otro equipo, número de dorsal) ahora comparan directamente contra `players` (vía `teams`), que es la fuente de verdad del roster actual — ya no contra `inscription_players`. Así, sin importar cuándo o cómo quedó huérfana una fila de `inscription_players`, un jugador borrado del roster deja de bloquear su propia CURP/número de inmediato, sin necesitar limpiar datos históricos con un script aparte.

*(Nota técnica: el primer intento de este fix unió `inscription_players` con `players` por CURP en vez de comparar directo contra `players` — funcionaba, pero daba un falso positivo si existían dos filas de `inscription_players` con la misma CURP en la misma categoría (una huérfana + una viva), porque el JOIN encontraba coincidencia "cruzada" entre ambas filas. Se detectó probando el ciclo completo end-to-end antes de dar el fix por bueno, y se corrigió comparando directo contra `players` sin pasar por `inscription_players` en la condición de duplicado.)*

**Nuevo endpoint:** `PUT /inscriptions/:id/players/:playerId` — permite al delegado (autenticado por el mismo `token` de registro que ya usa el alta, o admin) editar un jugador ya enviado: nombre, número, posición, CURP, foto y documento. Re-valida CURP (formato + edad vs. categoría) y duplicados con el mismo criterio "self-healing". Actualiza `inscription_players` y refleja el cambio en `players` (si el jugador real fue borrado, lo repone al guardar la edición — igual de auto-correctivo que el registro nuevo).

**Frontend:** en [InscriptionPlayers.vue](src/pages/InscriptionPlayers.vue), cada jugador ya registrado (la lista "Registrados" dentro de cada categoría) ahora tiene un botón "Editar" que convierte esa fila en un formulario inline (nombre, #, posición, CURP con la misma validación visual del alta, documento) con "Guardar cambios"/"Cancelar", llamando al nuevo endpoint PUT.

**Validado con:** backend local (SQLite) — se replicó el escenario exacto reportado insertando directamente en la base una inscripción+equipo+`inscription_players` huérfana (sin jugador vivo correspondiente), y se confirmó contra los endpoints reales: (1) `POST /inscriptions/:id/players` con la misma CURP ya NO se rechaza — se registra con éxito; (2) `PUT /inscriptions/:id/players/:playerId` edita correctamente nombre/CURP/documento y el cambio se refleja tanto en `inscription_players` como en `players`; (3) ciclo completo `DELETE /players/:id` (admin) → `POST /inscriptions/:id/players` (delegado, misma CURP) confirmado sin bloqueo. Build de producción limpio. No se pudo probar la subida real de un documento en el formulario de edición del delegado por las mismas credenciales placeholder de Cloudinary en local ya mencionadas antes — el endpoint de upload en sí no se tocó.

### 2026-07-13 — Auditoría completa de "seguir equipo/torneo" + notificaciones push
**Pedido:** el sistema de seguir equipo/torneo debe funcionar bien de punta a punta, y debe notificar de forma lógica: próximos partidos del equipo seguido, noticias del torneo, y eventos normales de progreso como "fuiste campeón" o "fuiste eliminado".

**Auditoría (agente en background, solo lectura) — hallazgos:**
1. **Modelo de datos correcto, no duplicado:** existen dos mecanismos de follow en paralelo — el legado por dispositivo (`team_follows`/`tournament_follows`, clave `endpoint`) y el de cuenta (`user_team_follows`/`user_tournament_follows`, clave `user_id`, agregado en esta sesión). No son redundantes: `sendPushToTeams`/`sendPushToTournaments` ya consultan ambos con `OR`, así un usuario logueado recibe push en cualquier dispositivo. El mecanismo legado ya no es alcanzable desde el UI actual (los botones de seguir exigen login), pero se deja intacto por compatibilidad — no se tocó.
2. **Bug crítico real:** el push de "⚽ Resultado final" solo existía en `PATCH /matches/:id/status` (ruta admin que casi no se usa). La ruta que **realmente** usan árbitros y admin para cerrar un partido con marcador (`PATCH /matches/:id/score` con `finish:true`, usada desde `RefereePortal.vue`, `admin/Referee.vue`, `MatchDetail.vue`, `admin/Matches.vue`) **nunca mandaba push** — en la práctica, el flujo real de finalizar partidos no notificaba a nadie.
3. **Cobertura de eventos:** de 10 tipos de notificación lógicos esperados, solo 2 existían (partido en vivo, noticia publicada) y el de "resultado final" estaba roto (punto 2). Cero cobertura para: recordatorio de próximo partido, eliminación, avance de ronda, campeón, premios automáticos (goleador/asistidor/portero), inicio de eliminatoria, torneo finalizado.
4. **Bug de bracket encontrado de paso (no relacionado a notificaciones, pero se corrigió porque bloqueaba las nuevas notificaciones de forma segura):** `advanceBracketWinner` no distinguía el partido de "Tercer Lugar" de un partido normal. Como el round "Tercer Lugar" tiene un `order_index` menor al de la Final, la búsqueda de "siguiente ronda" encontraba la Final y el código llegaba a **sobreescribir `home_team`/`away_team` de la Final** con el ganador del Tercer Lugar. No se había detectado antes porque el orden típico (Tercer Lugar después de la Final) rara vez dispara este camino, pero es reproducible. Se corrigió con un `if (currRound.name === 'Tercer Lugar')` que corta el flujo antes de la lógica de avance.

**Implementado (backend/routes/index.js):**
- `sendMatchFinishedPush(m)` / `sendMatchLivePush(m)`: helpers compartidos, usados ahora por las 3 rutas que pueden finalizar/iniciar un partido (antes cada una tenía su propio payload duplicado con textos ligeramente distintos).
- `PATCH /matches/:id/score` (la ruta real de árbitro/admin): ahora manda el push de resultado final cuando el partido se cierra por primera vez (no en correcciones posteriores de marcador).
- `advanceBracketWinner`: guard de Tercer Lugar (ver punto 4) + push de **🥉 Tercer lugar** (ganador), **🎉 ¡Avanzaste de ronda!** (ganador de ronda normal) y **Quedaste eliminado** (perdedor) en cada ronda, y en la Final: **🏆 ¡Campeón!** al equipo ganador, **🥈 Subcampeón** al perdedor, y **🏆 Ya tenemos campeón** a todos los seguidores del torneo.
- `autoGenerateAwardsForPhase`: push automático cuando se genera cada premio — **⚽ Goleador**, **🎯 Máximo asistidor**, **🧤 Mejor portero** (liga/grupos) y **⚽ Goleador de la eliminatoria** (knockout), dirigido a los seguidores del equipo del premiado. El premio de campeón (`best_team`) NO manda push aquí porque `advanceBracketWinner` ya lo hizo al terminar la Final (evita duplicado).
- `checkPhaseCompletion`: cuando una fase de grupos termina y se genera el bracket de eliminatoria automáticamente, push **🏁 Comienza la eliminatoria** a los seguidores del torneo.
- `checkUpcomingMatchReminders()`: nueva función que revisa partidos `scheduled` con `date` dentro de las próximas 2 horas y manda **⏰ Tu equipo juega pronto** a los seguidores de ambos equipos, marcando `reminder_sent=1` para no repetir. Como el proyecto no tiene ninguna infraestructura de cron, se expone en `global.checkUpcomingMatchReminders` y se llama desde un `setInterval` en `server.js` cada 15 minutos (mismo patrón que los keep-alive existentes). Columna nueva `matches.reminder_sent` agregada a `db-schema.js` (Postgres) y `db-sqlite-init.js` (SQLite).

**Validado con:** backend local (SQLite) con un log temporal en `sendPush` (removido antes de commitear) que imprimía tipo/audiencia de cada push disparado. Se armó a mano un torneo de prueba completo con 4 equipos, semifinales, Tercer Lugar y Final, y un usuario "fan" con follows reales (`user_team_follows`/`user_tournament_follows` + una `push_subscriptions` falsa) para verificar la audiencia exacta de cada evento:
- Equipo eliminado en semifinal → push solo a sus seguidores (audience=1), el ganador recibe "avanzaste" (audience=0 porque nadie lo seguía).
- Partido de Tercer Lugar finalizado → confirmado que la Final (ya creada, en estado `scheduled`) **no se alteró** (el bug del punto 4 habría sobreescrito sus equipos) — push de "🥉 Tercer lugar" correcto.
- Final finalizada → campeón, subcampeón y campeón-de-torneo disparados con la audiencia correcta.
- Partido de fase de grupos con un gol registrado (`match_events`) → al ser el único partido de la fase, se completó sola, generando el premio de Goleador y Mejor Portero con push, más el aviso de "Comienza la eliminatoria" (por ser fase tipo `groups`).
- Recordatorio: partido `scheduled` a 1 hora → push de recordatorio disparado una sola vez; una segunda llamada al job confirmó que `reminder_sent=1` evita el reenvío.
- Sin errores en los logs del servidor durante toda la prueba. Datos de prueba limpiados de la base local al terminar. Build de producción limpio.

**No modificado (fuera de alcance, documentado para referencia futura):**
- El mecanismo legado de follow por `endpoint` (sin cuenta) — funcional pero inalcanzable desde el UI actual, se deja por compatibilidad.
- Torneos de modalidad "liga" pura (sin fase de eliminatoria) no generan un award de "campeón" — el sistema de premios automáticos solo asigna `best_team` en fases de tipo `knockout`; determinar un campeón de liga por posición en la tabla es una función distinta que no existía antes y no se agregó aquí.
- Posible push duplicado para un usuario que sigue tanto el equipo como el torneo de un mismo partido (recibe el aviso de "resultado final" dos veces, una por cada follow) — comportamiento preexistente, de bajo impacto, no se tocó en esta pasada.

### 2026-07-13 — UI: separar CURP y estadísticas en "Jugadores y Responsables"
**Pedido:** en la tabla de la pestaña "Jugadores" del admin, mostrar la CURP en vez de goles/asistencias/tarjetas, y mover esas estadísticas a una pestaña nueva llamada "Rendimiento". El PDF exportado también debe mostrar CURP en vez de las estadísticas.

**Cambios en [Players.vue](src/pages/admin/Players.vue):**
- Nueva pestaña **"Rendimiento"** (entre "Jugadores" y "Responsables") con su propia vista (cards en mobile, tabla en desktop) mostrando Goles/Asistencias/T. Amarillas/T. Rojas por jugador, reutilizando los mismos filtros de torneo/categoría/equipo y el botón "Editar" (abre el mismo modal, que ya tenía la sección de estadísticas manuales).
- La tabla de la pestaña "Jugadores" ahora muestra una columna **CURP** en vez de las 4 columnas de estadísticas (mobile: la CURP se agregó como línea bajo el nombre del jugador, reemplazando el badge de goles).
- `exportPDF()`: la columna final del PDF ahora es CURP en vez de G/A/TA/TR.

**Validado con:** build de producción limpio y recorrido en el navegador contra el backend local — la pestaña "Jugadores" muestra CURP (o "—" si no tiene), la pestaña "Rendimiento" muestra las estadísticas correctamente con el filtro de equipo funcionando igual que antes, sin errores de consola.

### 2026-07-13 — Campeón automático para torneos de liga pura (hallazgo pendiente de la auditoría de notificaciones)
**Pedido:** implementar el hallazgo que se había dejado como chip aparte durante la auditoría de notificaciones: los torneos de modalidad "liga" (sin fase de eliminatoria) nunca coronaban un campeón automático, así que tampoco podían avisar a los seguidores quién ganó.

**Solución en `autoGenerateAwardsForPhase` ([backend/routes/index.js](backend/routes/index.js)):** dentro de la rama que ya generaba goleador/asistidor/portero para fases `league`/`groups`, se agregó un bloque exclusivo para `phase.type === 'league'` (no aplica a `groups`, que en cambio avanza a una eliminatoria donde el campeón real se decide en la Final) que consulta la tabla `standings` de esa fase (`ORDER BY points DESC, diferencia de goles DESC, goles a favor DESC LIMIT 1` — los mismos criterios de desempate que ya usa la tabla de posiciones visible en el sitio) para determinar el 1er lugar, inserta el award `best_team` (igual que ya hace la rama `knockout`), y manda el mismo push de **🏆 ¡Campeón!** / **🏆 Ya tenemos campeón** a los seguidores del equipo y del torneo.

**Validado con:** backend local (SQLite) con el mismo log temporal de auditoría (removido antes de commitear). Se armó un torneo de prueba con modalidad "liga", 2 equipos, 1 partido (única jornada de la fase), y un usuario "fan" siguiendo al equipo ganador y al torneo. Al finalizar el partido (3-1), se confirmó: standings quedó con el equipo ganador en 3 pts vs 0 del perdedor, se insertó el award `best_team` con la descripción "Campeón de Liga Regular (3 pts)", y el push de campeón se disparó tanto a nivel equipo como torneo con audiencia=1 (exactamente el fan que seguía a ambos). Build de producción limpio.

### 2026-07-13 — El PDF de jugadores debe incluir la foto
**Pedido:** que el PDF exportado desde "Jugadores" (pestaña principal, botón "Exportar PDF") incluya la foto de cada jugador.

**Cambios en `exportPDF()` ([Players.vue](src/pages/admin/Players.vue)):**
- Nueva columna **Foto** (primera) — muestra un `<img>` circular con la foto real si el jugador tiene una, o un círculo placeholder con la inicial del nombre si no tiene.
- Como las fotos son URLs remotas (Cloudinary o `/uploads` local), se agregó una espera a que todas las `<img>` de la ventana del PDF terminen de cargar (`load`/`error`) antes de llamar a `win.print()` — antes se imprimía a los 400ms fijos, lo que hubiera dejado las fotos en blanco por la carrera entre la descarga de la imagen y el print. Tiene un timeout de seguridad de 3s por si alguna imagen nunca dispara el evento.

**Validado con:** build de producción limpio. Como `window.open()` es bloqueado por el navegador cuando el click se dispara vía automatización (no cuenta como gesto de usuario confiable), se verificó interceptando temporalmente `window.open` para capturar el HTML generado en un iframe visible en vez de una ventana emergente real — confirmado que la tabla trae la columna Foto primero, con la imagen real (`<img src=".../uploads/....jpeg">`) para un jugador con foto y el placeholder con inicial ("G") para uno sin foto, y que `print()` se llamó correctamente después de que la imagen cargara. Captura de pantalla confirma el círculo con la foto real renderizado correctamente junto al placeholder.

### 2026-07-13 — Botón "Instalar app" del Hero: un toque en Android/Chrome/Edge, instrucciones visuales en iOS
**Pedido:** que el botón "Instalar app" del Hero evite, cuando sea técnicamente posible, que el usuario tenga que ir al menú de los 3 puntos → compartir → añadir a inicio. El usuario ya sabía (y compartió el detalle técnico) que esto depende 100% del navegador/SO: Chrome/Edge/navegadores basados en Chromium exponen `beforeinstallprompt`, que permite instalar con un solo toque; Safari/iOS no expone ninguna API para eso (restricción de Apple, no del código), así que ahí la única opción es mostrar instrucciones visuales claras del paso manual — pidió implementar exactamente esa "Opción 2": detectar iOS y mostrar un modal con "📲 Instala la aplicación", pasos numerados grandes y tomando en cuenta que ya existe un tutorial en video (para no duplicar ni romper eso).

**Estado previo:** [usePWA.js](src/composables/usePWA.js) ya escuchaba `beforeinstallprompt` y exponía `promptInstall()`, pero no había ninguna detección de iOS, y el botón del Hero (`scrollToInstall()`) siempre bajaba al video tutorial sin importar el navegador — nunca usaba el prompt nativo aunque estuviera disponible.

**Solución:**
- `usePWA.js`: nuevo `isIOS` (regex sobre `navigator.userAgent`, cualquier navegador en iOS cuenta porque todos corren sobre WebKit y comparten la misma limitación — no es exclusivo de Safari). También se sumó `navigator.standalone === true` a la detección de `isInstalled` (antes solo miraba `display-mode: standalone`, que no aplica en iOS).
- Nuevo componente [IOSInstallModal.vue](src/components/InstallPWA/IOSInstallModal.vue) — reutiliza el sistema de modales del proyecto (`.modal-overlay`/`.modal-sheet`/`.modal-header`/`.modal-body`/`.modal-footer`, el mismo patrón de `DocumentViewerModal.vue`) con 2 pasos grandes con ícono + texto (Compartir → Añadir a pantalla de inicio) y un botón "Ver video paso a paso" que cierra el modal y baja al tutorial en video ya existente (`InstallTutorial.vue`, sección `#instalar-app`) — sin duplicar ese contenido, solo lo referencia.
- [HeroSection.vue](src/components/Hero/HeroSection.vue): el botón "Instalar app" ahora llama a `handleInstallClick()`, que decide entre 3 caminos: (1) si `installPrompt` está disponible (Android/Chrome/Edge/Chromium) → `promptInstall()` directo, un solo toque; (2) si es iOS y no está ya instalada → abre `IOSInstallModal`; (3) cualquier otro caso (ya instalada, navegador sin soporte) → cae al comportamiento anterior (`scrollToInstall()`, baja al video).

**Validado con:** build de producción limpio. Probado en el navegador (viewport móvil 375×812): (1) forzando la condición de iOS temporalmente para la prueba (revertido antes de commitear) — el modal abre con los 2 pasos visuales, el botón "Ver video paso a paso" cierra el modal y baja correctamente hasta los videos de instalación (confirmado por captura de pantalla mostrando la sección con los thumbnails de Android/iPhone), y tanto la X como "Entendido" cierran el modal sin bajar; (2) disparando un evento sintético `beforeinstallprompt` (simulando Android/Chrome real) — confirmado que el click en "Instalar app" llama a `.prompt()` directamente y NO abre el modal de iOS. Sin errores de consola en ningún escenario.

### 2026-07-13 — Auditoría: activar/desactivar notificaciones + distinguir equipos de torneos seguidos
**Pedido (con capturas):** revisar que el activado/desactivado de notificaciones push funcione bien, y que la UI distinga entre equipos y torneos seguidos — la captura mostraba el menú de usuario diciendo "Siguiendo 2 equipos" sin mencionar torneos en absoluto.

**Auditoría (agente en background, solo lectura) — 2 hallazgos reales:**
1. **Bug real de "Desactivar notificaciones":** el permiso de notificaciones del navegador (`Notification.permission`), una vez concedido, **no se puede revocar por JavaScript** — solo se puede borrar la suscripción propia de la app. [usePWA.js](src/composables/usePWA.js) leía `pushGranted` directo de `Notification.permission === 'granted'` en cada `onMounted`, sin recordar que el usuario había desactivado explícitamente. Consecuencia real: tras pulsar "Desactivar notificaciones" y luego **recargar la página** (o navegar a otra), la campana volvía a mostrarse "activa" aunque ya no hubiera ninguna suscripción real (`push_subscriptions` borrado, `pushEndpoint`/`pushSub` en null) — y peor, si el usuario intentaba desactivar de nuevo en ese estado, `unsubscribePush()` hacía `return` temprano al no encontrar ningún `endpoint` (ya estaba en null), **sin resetear `pushGranted`** — el botón parecía no hacer nada.
2. **"Siguiendo N equipos" no distinguía torneos:** [following.js](src/stores/following.js) ya trackeaba `teamCount`/`tournamentCount` por separado correctamente, pero el único texto visible en el menú de usuario ([AppNavbar.vue](src/components/Navbar/AppNavbar.vue)) usaba `count` (la suma combinada) con la palabra "equipo(s)" fija — un usuario siguiendo 1 equipo + 1 torneo veía "Siguiendo 2 equipos", dato incorrecto.

**Solución:**
- `usePWA.js`: nuevo flag persistido `jlp_push_disabled` en localStorage. Al montar, `pushGranted` ahora es `Notification.permission === 'granted' && no está marcado como desactivado` — así una desactivación explícita se respeta entre recargas, sin depender de un permiso de navegador que nunca se puede revocar de verdad. `subscribePush()` limpia el flag al reactivar con éxito. `unsubscribePush()` ahora resetea el estado local (`pushSub`, `pushGranted`, `pushEndpoint`) y marca el flag **siempre**, incluso si no había un `endpoint` guardado — la llamada al backend sigue siendo condicional (no tiene sentido avisar al servidor de un endpoint que no existe), pero el estado local de la UI ya no se queda pegado en "activo".
- `following.js`: nuevo getter `summaryText`, arma "N equipo(s)" y/o "N torneo(s)" según corresponda (omite la parte que esté en cero), unidos con "y".
- `AppNavbar.vue`: el texto del menú de usuario ahora usa `following.summaryText` en vez del conteo combinado con la palabra fija "equipos".

**Validado con:** (1) se evaluó directamente la expresión corregida con `Notification.permission` simulado a `'granted'` — con el flag limpio da `true` (activo) y con el flag puesto a `'true'` da `false` (inactivo), confirmando que ya no depende solo del permiso crudo; (2) se creó un usuario "fan" real en el backend local (SQLite) con contraseña hasheada, siguiendo 1 equipo real (LEON) y 1 torneo real (COPA CARIBE) vía `user_team_follows`/`user_tournament_follows`, se inició sesión en el navegador y se confirmó por captura de pantalla que el menú de usuario ahora muestra exactamente "Siguiendo 1 equipo y 1 torneo". Build de producción limpio, sin errores de consola. Datos de prueba limpiados de la base local al terminar.

### 2026-07-13 — Fix real: el registro de jugadores rechazaba archivos HEIC (fotos de iPhone)
**Pedido:** el usuario compartió una carpeta real (`~/Downloads/CLUB ATLETICO RIVER´S`) con credenciales, cartas y actas de ~19 jugadores para dar de alta — reportó que "no acepta algunos archivos" y pidió hacer la subida de archivos lo más robusta posible.

**Causa raíz encontrada al revisar la carpeta real:** la mayoría de las credenciales/cartas/actas de esos jugadores están en formato **`.heic`/`.HEIC`** (el formato nativo de fotos de iPhone). El `fileFilter` de multer en `backend/routes/index.js` (usado por `POST /upload` y `/upload/public`, es decir **todas** las subidas de archivo de la app) tenía una lista blanca de extensiones que **no incluía `.heic`/`.heif`** — por eso se rechazaban silenciosamente con "Archivo no válido" antes de siquiera intentar subirlos a Cloudinary.

**Bug adicional encontrado de paso:** `compressFile()` en `src/utils/upload.js` (usado por el registro público de inscripciones y por `uploadImage`) comprime imágenes dibujándolas en un `<canvas>` vía `<img>`. Para HEIC, si el navegador SÍ reporta `file.type: 'image/heic'` (p.ej. Safari), el código entraba a ese flujo de compresión — pero **ningún navegador puede decodificar HEIC en un `<img>`**, así que `img.onload` nunca se disparaba y, al no haber `img.onerror`, la promesa se quedaba colgada para siempre (spinner infinito, sin error visible). En Chrome, donde `file.type` suele venir vacío para HEIC, el bug de multer bloqueaba antes de llegar aquí, pero en Safari este segundo bug hubiera bloqueado igual aunque se arreglara multer.

**Solución (frontend + backend):**
- `backend/routes/index.js`: se agregó `.heic`, `.heif`, `.bmp`, `.tiff`, `.tif` a la lista blanca de multer. Cloudinary ya soporta HEIC como formato de entrada y lo convierte a JPEG/WebP en la entrega gracias a `fetch_format:'auto'` (ya activo en `uploadToCloudinary` para imágenes) — el único obstáculo real era este filtro.
- `src/utils/upload.js`: nuevas funciones `isImageFile()` (detecta imagen por MIME **o** por extensión `.heic`/`.heif`, ya que el MIME no es confiable para HEIC en varios navegadores) e `isHeic()`. `compressFile()` ahora salta la compresión para HEIC (se sube tal cual, sin intentar decodificarlo) y se le agregó `img.onerror`/`reader.onerror` como red de seguridad para no dejar la promesa colgada ante cualquier imagen no decodificable.
- Se actualizaron los `accept` de **todos** los `<input type="file">` de imagen/documento del proyecto (`Players.vue` admin, `InscriptionPlayers.vue` público, `ImageUpload.vue`, `Gallery.vue`, `News.vue`, `Inscription.vue`) para incluir explícitamente `.heic,.heif` — en algunos navegadores/SO, `accept="image/*"` no reconoce HEIC como imagen y lo oculta del selector de archivos nativo.
- Se reemplazaron todas las validaciones sueltas `file.type.startsWith('image/')` (vulnerables al mismo problema de MIME vacío) por `isImageFile()` en `Players.vue`, `ImageUpload.vue`, `Gallery.vue` y `News.vue`.

**Validado con:** backend local (SQLite, credenciales de Cloudinary son placeholders ahí) — se subió un archivo `.HEIC` real de la carpeta del usuario (credencial de Bruno Cahuich) contra `POST /upload`: antes del fix, multer lo rechazaba con `400 Archivo no válido`; después del fix, pasa el filtro y llega hasta Cloudinary, fallando solo por `Unknown API key tu_api_key` (la credencial falsa del entorno local) — se confirmó que un `.jpg` y un `.pdf` reales de la misma carpeta fallan con el **mismo** error, probando que el HEIC ya se trata igual que cualquier otro archivo válido y ya no se bloquea de forma especial. La lógica de detección (`isImageFile`/`isHeic`) se verificó con casos representativos de MIME vacío (Chrome) y MIME correcto (Safari) para `.heic`/`.HEIC`, `.jpg`, `.pdf` y un archivo no-imagen — todos los resultados correctos. Build de producción limpio.

### 2026-07-13 — Botón "Agregar otro jugador" debajo de cada fila + fix de bug bloqueante en dev local
**Pedido:** en el registro público de jugadores, agregar un botón para añadir otro jugador justo debajo de cada fila ya cargada en el formulario, para no tener que subir hasta el botón de la cabecera de la sección cada vez.

**Cambio en [InscriptionPlayers.vue](src/pages/InscriptionPlayers.vue):** el botón grande "+ Agregar jugador en {categoría}" solo aparecía cuando el formulario estaba completamente vacío (`v-if="!newPlayers[cat.id]?.length"`) — en cuanto había una fila cargada, ese botón desaparecía y la única forma de agregar otra era subir hasta el botón fijo de la cabecera de la sección. Se agregó un botón "+ Agregar otro jugador" que aparece justo debajo de la(s) fila(s) actual(es) en cuanto ya hay al menos una cargada, complementando (sin reemplazar) el botón de la cabecera.

**Bug bloqueante encontrado al intentar probar esto en local:** para verificar el cambio en el navegador hacía falta una inscripción aprobada real, pero tanto crear una (`POST /inscriptions`) como visualizar la página de registro (`GET /inscriptions/:id/register`) fallaban en SQLite local con `unrecognized token: ":"` — el mismo bug de `ANY($1::bigint[])` (sintaxis de array exclusiva de Postgres) que ya se había identificado como pendiente en una sesión anterior, ahora bloqueando además la vista de registro pública, no solo el alta de inscripciones. Como el patrón de solución (`IN (...)` con placeholders dinámicos) ya estaba probado en el mismo archivo para casos similares, se corrigieron los 3 usos de `ANY($1::bigint[])` restantes en `backend/routes/index.js` (`POST /inscriptions`, `createTeamsFromInscription`, `GET /inscriptions/:id/register`) con ese mismo patrón portable.

**Validado con:** backend local (SQLite) — se creó una inscripción aprobada de prueba directamente en la base (para evitar depender del endpoint que se estaba arreglando en el mismo cambio) y se navegó a la página de registro real: cargó correctamente (antes daba "Acceso no disponible" con el error de SQLite). Se hizo clic en "+ Agregar jugador" para cargar la primera fila, se confirmó que "+ Agregar otro jugador" aparece debajo, se hizo clic de nuevo y se confirmó por el DOM que había 2 filas de jugador cargadas. Sin errores de consola. Build de producción limpio. Datos de prueba limpiados de la base local al terminar.

### 2026-07-13 — Fix real: el botón "Guardar jugadores" se bloqueaba al registrar varios a la vez
**Reporte del usuario:** al agregar varios jugadores a la vez en el registro público, a veces después de cargar varias filas el botón de guardar se bloqueaba y no dejaba guardar nada.

**Causa raíz:** la condición `:disabled` del botón "Guardar jugadores en {categoría}" era `newPlayers[cat.id]?.some(p => p.name.trim() && curpStatus(p) !== 'valid')` — es decir, bastaba con que **cualquier** fila tuviera un nombre escrito con una CURP todavía incompleta o inválida para bloquear el guardado de **todas** las filas, incluidas las que ya estaban completas y listas. Como el flujo natural es escribir el nombre y luego la CURP, en cuanto el usuario empezaba la fila #3 (nombre puesto, CURP a medias) el botón se bloqueaba aunque las filas #1 y #2 ya estuvieran perfectamente completas.

**Solución:**
- El botón ahora se habilita en cuanto **al menos una** fila esté lista (nombre + CURP válida), en vez de bloquearse por cualquier fila incompleta: `:disabled="saving[cat.id] || !newPlayers[cat.id]?.some(p => p.name.trim() && curpStatus(p) === 'valid')"`.
- `savePlayers()` ahora solo envía al backend las filas que están completas (nombre + CURP válida) — las filas a medias se quedan intactas en el formulario para que el usuario las siga llenando, en vez de mandarlas también y que reboten con "la CURP es obligatoria".
- Se agregó un aviso pequeño debajo del botón ("Se guardarán solo los jugadores con nombre y CURP válidos...") cuando hay filas a medias, para que quede claro qué va a pasar al guardar.

**Validado con:** backend local (SQLite) — se cargaron 2 filas en el formulario, la primera completa (nombre + CURP válida) y la segunda a medias (solo nombre, CURP vacía). Se confirmó por JS (`btn.disabled === false`) que el botón ya NO se bloquea con esa combinación (antes del fix sí se hubiera bloqueado). Se hizo clic en Guardar y se confirmó que el jugador completo pasó a la lista de "Registrados" mientras que el jugador a medias permaneció intacto en el formulario, listo para completarse. Sin errores de consola. Build de producción limpio. Datos de prueba limpiados de la base local al terminar.

### 2026-07-13 — Auditoría completa del área de registro (equipo + jugadores + responsables)
**Pedido:** revisar que no haya errores de ningún tipo en toda el área de registro y que todo fluya bien — auditoría completa, no solo el bug puntual reportado antes.

**Auditoría (agente en background, solo lectura):** revisó `Inscription.vue` (alta inicial de equipo), `InscriptionPlayers.vue` completo (responsables + jugadores + la edición agregada esta sesión), `src/utils/upload.js`, y todas las rutas de `/inscriptions*` en el backend. Encontró 5 bugs reales confirmados y 12 hallazgos de menor prioridad (varios de ellos "revisado, no es bug" — ver detalle abajo). De los 5 reales, se corrigieron 4 (bajo riesgo, alto valor); el quinto (falta de constraints únicos + transacciones en la BD, ver nota al final) se dejó documentado sin tocar por ser un cambio estructural de mayor riesgo.

**Corregido:**
1. **Aviso de edad ausente en el cliente** — el backend valida la CURP contra el rango de nacimiento de la categoría (y la excepción de niñas) al guardar, pero el formulario no tenía ninguna señal de esto: la CURP se veía "✓ válida" (formato correcto) y el rechazo por edad solo aparecía como error *después* de darle a Guardar — la misma clase de "affordance engañoso" que el bug del botón ya corregido, pero para la validación de edad en vez del formato. Se agregó `curpAgeWarning(player, cat)` en [InscriptionPlayers.vue](src/pages/InscriptionPlayers.vue), un aviso informativo (no bloquea nada, el backend sigue siendo la autoridad final porque además cuenta el cupo de la excepción de niñas, algo que no se puede replicar sin consultar al servidor) que avisa en el momento si la fecha de nacimiento de la CURP no encaja con el rango de la categoría — tanto en el alta de jugadores nuevos como en la edición de uno ya registrado.
2. **Edición de jugador se descartaba en silencio al cambiar de categoría/jugador sin guardar** — `editingPlayerId` es un solo valor global a toda la página (no por categoría), así que abrir "Editar" en un jugador de otra categoría (o de la misma) reasignaba el id sin avisar, perdiendo cualquier cambio sin guardar en la edición anterior. Se agregó `editOriginal` (foto de los valores con los que se abrió la edición actual) + `hasUnsavedEditChanges()`, y `startEditPlayer()` ahora pide confirmación antes de descartar una edición con cambios pendientes — pero NO pregunta si no hay cambios reales, para no ser molesto.
3. **Dorsal `0` se trataba como "sin número" y se saltaba la validación de duplicados** — `p.number ? parseInt(p.number) : null` coerciona `0` a `null` por ser falsy en JS. La UI no permite 0 (`min="1"` en el input), pero una llamada directa a la API sí podía colarlo. Se corrigió la asignación (`p.number != null && p.number !== '' ? ... : null`) en `POST /inscriptions/:id/players` y `PUT /inscriptions/:id/players/:playerId` — y se encontró **el mismo bug un nivel más abajo** al probarlo: el `if (num)` que decide si se valida el duplicado *también* trataba 0 como falsy y se saltaba el chequeo aunque el valor ya se guardara bien. Se corrigió a `if (num != null)` en ambas rutas.
4. **Ninguna ruta pública verificaba que la categoría enviada fuera una de las que la inscripción realmente registró** — `POST /inscriptions/:id/players` y `POST /inscriptions/:id/responsables` solo validaban que la categoría perteneciera al *torneo*, no a `insc.categories_json` (las categorías específicas de esa inscripción). Con el token de un equipo, una llamada directa a la API podía mandar jugadores/responsables a una categoría que ese equipo nunca seleccionó, generando registros huérfanos sin equipo real. Se agregó la validación contra `categories_json` (con `category_id` como respaldo) en ambas rutas — solo bloquea cuando sí hay datos de categorías que comparar, para no romper inscripciones antiguas con datos raros.
5. **Multer devolvía 500 en vez de 413 cuando un archivo excede el límite de tamaño** — `MulterError` no trae `.status`/`.statusCode`, así que el manejador de errores global caía al 500 genérico. El mensaje sí le llegaba bien al usuario (el frontend ya lo detecta por texto), pero cualquier monitoreo de errores 5xx lo contaría como falla real del servidor en vez de un rechazo esperado del cliente. Se agregó un caso especial en `backend/server.js` para `MulterError` con `code:'LIMIT_FILE_SIZE'` → 413.

**No corregido (documentado, no es parte de esta pasada):**
- **Ninguna tabla tiene constraints únicos** (`players.curp`, `players(team_id, number)`, `inscriptions(tournament_id, team_name)`) y no hay transacciones — todo chequeo de duplicado (incluidos los "self-healing" corregidos en sesiones anteriores) sigue siendo un best-effort "SELECT para revisar, luego INSERT", no una garantía, bajo acceso concurrente real (dos pestañas o dispositivos mandando el mismo dato al mismo tiempo). Es un cambio estructural (migración de esquema + posible limpieza de duplicados ya existentes) que amerita su propia sesión dedicada, no un fix de pasada.
- Subidas huérfanas a Cloudinary al cancelar una edición o quitar una fila con foto/documento ya subido — solo desperdicio de almacenamiento, no rompe nada funcionalmente.
- El resto de los hallazgos (doble-submit ya bien protegido, manejo de rutas 4xx genéricas, `:key="idx"` en los v-for de filas, límites de tamaño frontend/backend) se revisaron y **no** resultaron ser bugs reales.

**Validado con:** backend local (SQLite). Se probó cada fix contra el endpoint real: (1) aviso de edad — se configuró temporalmente un rango de edad en una categoría real y se confirmó que el aviso aparece en el DOM con una CURP fuera de rango; (2) confirmación al cambiar de edición — se registraron 2 jugadores reales, se abrió edición del primero, se cambió el nombre sin guardar, se intentó editar el segundo y se confirmó que `window.confirm` se dispara con el mensaje esperado (interceptado para la prueba) — y se confirmó por separado que **no** se dispara si no hay cambios pendientes; (3) dorsal 0 — confirmado que se guarda como `0` (no `null`) y que un segundo jugador con el mismo dorsal 0 en la misma categoría/equipo ahora sí se rechaza como duplicado (antes del segundo fix no se detectaba); (4) categoría ajena — confirmado que una inscripción restringida a la categoría 28 recibe `400 Esta categoría no forma parte de tu inscripción` al intentar registrar en la categoría 29, y que la categoría 28 (la correcta) sigue funcionando normal. Sin errores de consola en ningún escenario. Build de producción limpio. Categorías y datos de prueba restaurados/limpiados en la base local al terminar.

### 2026-07-14 — Verificación end-to-end del área de registro (sin cambios)
**Pedido:** "revisa que todo este funcional y nada falle del registro en general" — verificación funcional completa, no otra auditoría de código.

**Validado con:** flujo real completo a través del navegador (no curl/DB directo) contra backend local: alta pública de equipo (`Inscription.vue`) → aprobación vía API real → registro de responsables (con foto simulada, ya que Cloudinary usa credenciales placeholder en local) → registro de jugadores con filas mixtas completas/incompletas (confirma que el botón "Guardar" ya no se bloquea) → dorsal `#0` aceptado y su duplicado rechazado → aviso de edad aparece/desaparece según la CURP → edición de jugador con diálogo de confirmación al cambiar de fila sin guardar → panel admin `Players.vue` refleja los 2 jugadores y 2 responsables correctamente. Ningún fallo nuevo encontrado — los 5 fixes de la auditoría anterior se comportan bien juntos en un flujo real combinado. Sin cambios de código; datos de prueba y configuración local limpiados al terminar.

### 2026-07-14 — Contador de seguidores en equipos y torneos
**Pedido:** "agrega un contador de followers ... cuantas personas siguen el torneo y cuantas al equipo etc, todo lo que se pueda seguir que tenga un contador de followers".

**Contexto:** el seguimiento de equipos/torneos ya existía (tablas `team_follows`/`tournament_follows` por dispositivo para push, y `user_team_follows`/`user_tournament_follows` por cuenta cuando hay sesión — ver entradas anteriores), pero en ningún lado se mostraba cuántas personas seguían cada cosa.

**Implementado:**
- [backend/utils/followers.js](backend/utils/followers.js) (nuevo) — `teamFollowerCount`/`tournamentFollowerCount` (un id) y `teamFollowerCounts`/`tournamentFollowerCounts` (lote, para listados, evita N+1). La cuenta deduplica correctamente: un mismo usuario logueado con push activo en ese navegador genera fila en *ambas* tablas (por dispositivo y por cuenta) para el mismo follow — sumar el tamaño de las dos tablas lo contaría dos veces. Se identifica cada seguidor por `user_id` cuando el endpoint del dispositivo tiene uno conocido (vía `push_subscriptions.user_id`), y por el endpoint mismo si no — un `UNION` sobre esa identidad hace el dedupe. `CAST(...AS TEXT)` en vez de `::text` porque SQLite no entiende la sintaxis de cast de Postgres.
- `followerCount` agregado a: `GET /tournaments/:slug` y `GET /tournaments` ([tournaments.controller.js](backend/controllers/tournaments.controller.js)), `GET /teams/:id/profile` (dentro de `stats`), `GET /tournaments/:slug/teams` y `GET /teams` ([routes/index.js](backend/routes/index.js)).
- Frontend: stat "Seguidores" en el perfil público del equipo ([TeamDetail.vue](src/pages/TeamDetail.vue), quick info row, ahora 5 columnas) y en el hero del torneo ([TournamentHome.vue](src/pages/TournamentHome.vue), junto a Equipos/Jugadores/Partidos, solo visible si hay al menos 1 seguidor). Badge con corazón en las tarjetas de [admin/Teams.vue](src/pages/admin/Teams.vue) (sumado entre las categorías de un mismo equipo, ya que el admin agrupa por nombre) y [admin/Tournaments.vue](src/pages/admin/Tournaments.vue).

**Bug propio encontrado y corregido antes de terminar:** la primera versión de `teamFollowerCount`/`tournamentFollowerCount` reusaba `$1` dos veces en el mismo query pasando un solo parámetro — funciona en Postgres (permite reutilizar el mismo placeholder) pero no en SQLite, porque la conversión `$N→?` en `config/db.js` reemplaza cada *aparición* de `$1` por un `?` independiente, dejando el statement con más `?` que valores (`Too few parameter values were provided`). Se corrigió duplicando el parámetro con `$1`/`$2` — mismo patrón ya usado en `sendPushToTeams`/`sendPushToTournaments` de esta misma sesión.

**Validado con:** backend local (SQLite). Se siguió un equipo real desde 2 endpoints anónimos distintos → `followerCount` mostró 2; se agregó un follow por cuenta (admin logueado) → subió a 3; se simuló el caso doble-conteo (mismo admin con push_subscriptions vinculando su endpoint a su user_id, siguiendo por ambas vías) → se confirmó que el contador se quedó en 3 (no subió a 4), probando el dedupe. Se repitió la prueba análoga para torneo. Verificado visualmente en navegador: "SEGUIDORES: 2" en el perfil público del equipo, "SEGUIDORES: 1" en el hero del torneo, badge "❤️ 2"/"❤️ 1" en las tarjetas de admin/Teams.vue y admin/Tournaments.vue. Build de producción limpio. Datos de prueba (follows, push_subscriptions) limpiados en la base local al terminar.

### 2026-07-14 — 5 features nuevas tipo "contador de seguidores" para la experiencia pública
**Pedido:** tras el contador de seguidores de equipos/torneos, el usuario pidió implementar todas las recomendaciones dadas para seguir mejorando la experiencia del lado público: viendo en vivo, seguir jugadores, ranking de más seguidos, reacciones a partidos y contador de vistas en noticias/galería.

**1. "Viendo en vivo" en partidos** — sala de socket.io `match:${id}` (no requiere tabla nueva: el conteo es simplemente `io.sockets.adapter.rooms.get(...).size`). [backend/server.js](backend/server.js) agrega `join:match`/`leave:match`, y usa el evento `disconnecting` (no `disconnect`) porque `socket.rooms` todavía tiene las salas match:* en ese punto — en `disconnect` ya se perdieron y no hay forma de saber de qué partidos salir. [src/services/socket.js](src/services/socket.js) expone `joinMatchRoom`/`leaveMatchRoom`/`onMatchViewers`. [MatchDetail.vue](src/pages/MatchDetail.vue) se une a la sala al montar y muestra un ícono de ojo + conteo junto al estado "EN VIVO".

**2. Seguir jugadores individuales** — mismo patrón de dos capas (`player_follows` por dispositivo + `user_player_follows` por cuenta) ya usado para equipos/torneos. Se refactorizó [backend/utils/followers.js](backend/utils/followers.js) a dos factories (`followerCountFactory`/`followerCountsFactory`) para no triplicar la misma consulta SQL de dedupe al agregar la tercera entidad. Rutas `/follows/players`, `/follows/player/add`, `/follows/player/remove`. [PlayerDetail.vue](src/pages/PlayerDetail.vue) ahora tiene botón "Seguir" + contador de seguidores.

**3. Ranking de más seguidos** — sección nueva en [Home.vue](src/pages/Home.vue) con el top 5 de torneos y equipos por `followerCount`, usando los mismos endpoints ya existentes (`GET /tournaments`, `GET /teams`) — no hizo falta ruta nueva, solo ordenar y recortar en el cliente.

**4. Reacciones rápidas a partidos (👍🔥😮)** — tabla `match_reactions(match_id, emoji, voter_id)` con `UNIQUE(match_id, emoji, voter_id)`. Público, sin cuenta: `voter_id` es un id anónimo de dispositivo nuevo, generado y guardado en `localStorage` ([src/utils/anon.js](src/utils/anon.js)) — independiente del endpoint de push, porque no todos los visitantes aceptan notificaciones y las reacciones debían funcionar para cualquiera. `POST /matches/:id/reactions` hace toggle (inserta si no existía, borra si ya existía) y difunde el conteo actualizado por la misma sala `match:${id}` del punto 1 (`match:reactions`), así otros viendo el partido ven la reacción en tiempo real.

**5. Contador de vistas en noticias/galería** — columna `view_count` en `news` y `galleries`. `POST /news/:id/view` y `POST /galleries/:id/view` (públicas). En [Media.vue](src/pages/Media.vue) se incrementa una sola vez por sesión (Set en memoria, no localStorage — se resetea al recargar, evita inflar el contador si el usuario abre/cierra la misma noticia varias veces) al expandir una noticia o abrir el lightbox de una foto. Importante: la mutación del contador se hizo sobre el array fuente (`news.value`/`galleries.value`), no sobre el item derivado del `computed` `feed` — mutar el derivado no dispara una recomputación porque Vue no lo trackea como dependencia reactiva propia.

**Alcance deliberadamente no incluido:** push notifications específicas por jugador seguido (ej. avisar cuando anota) — el pedido original era el contador de seguidores en sí, no una nueva categoría de notificaciones; se puede añadir después siguiendo el patrón de `sendPushToTeams`/`sendPushToTournaments` si se pide.

**Bug propio encontrado y corregido antes de terminar:** al probar el "viendo en vivo" en el navegador, el socket seguía conectándose a la URL de producción (Railway) en vez de a `localhost:3000` porque `VITE_SOCKET_URL` está fijado en el `.env` raíz del repo (no solo en `.env.production`) y `.env.local` no lo sobreescribía — a diferencia de `VITE_API_URL`, que sí estaba en `.env.local` desde sesiones anteriores. Se agregó `VITE_SOCKET_URL=http://localhost:3000` al `.env.local` temporal de la sesión (nunca al repo) para poder probar en local.

**Validado con:** backend + frontend locales (SQLite), con `VITE_SOCKET_URL` apuntando a localhost. Se forzó un partido real a estado `live` temporalmente: el contador de "viendo en vivo" mostró 👁 1 correctamente. Reacciones: clic en 👍 y 🔥 alternó el estado (toggle on/off confirmado via 2 clics de prueba) y mostró el conteo correcto en la UI. Seguir jugador: botón + "1 seguidor" aparecieron tras el clic en un jugador real. Ranking: se siguieron un equipo y un torneo reales desde curl, y la sección "Los más seguidos" del Home los mostró correctamente ordenados con su conteo. Vistas: se creó una noticia y galería de prueba reales, se expandieron/abrieron desde la UI pública, `view_count` subió a 1 en la base, y el badge "👁 1" apareció tanto en la vista pública como en admin/News.vue y admin/Gallery.vue. Build de producción limpio. Todo el partido de prueba, follows, reacciones, noticia y galería de prueba se limpiaron de la base local al terminar; `.env.local` (con `VITE_SOCKET_URL`) se borró y `backend/.env` se revirtió a `FRONTEND_URL=http://localhost:5173`.

### 2026-07-14 — Reaccionar a partidos ahora requiere sesión iniciada (como aficionado)
**Pedido:** "solo logueados como aficionados puedes hacer estas nuevas reacciones, si intentan hacerlo mandalos al login" — restringir las reacciones rápidas (👍🔥😮) recién agregadas a usuarios con sesión iniciada, redirigiendo al login a quien intente reaccionar sin estar logueado.

**Antes:** `POST /matches/:id/reactions` era pública y usaba un `voter_id` anónimo generado en el navegador ([src/utils/anon.js](src/utils/anon.js), ahora eliminado) — cualquiera podía reaccionar sin cuenta, y hasta reaccionar varias veces solo abriendo una ventana de incógnito nueva.

**Cambios:**
- `POST /matches/:id/reactions` ahora exige `authMiddleware` (sesión válida) en [backend/routes/index.js](backend/routes/index.js) — el `voter_id` pasó de ser un id anónimo de cliente a `String(req.user.id)`, ligando el voto a la cuenta en vez del dispositivo. `GET /matches/:id/reactions` se mantiene público (cualquiera puede ver el conteo) pero usa `optionalAuth` para calcular `mine` solo si hay sesión.
- [MatchDetail.vue](src/pages/MatchDetail.vue): `toggleReaction()` revisa `auth.isLoggedIn` antes de llamar a la API — si no hay sesión, redirige a `/login?redirect=<ruta actual>` (mismo patrón `?redirect=` que ya usaba Login.vue) en vez de intentar la llamada, que de todas formas rebotaría con 401. Se agregó un `title` tooltip ("Inicia sesión para reaccionar") en los botones para quien no tiene sesión.
- Se eliminó `src/utils/anon.js` — quedó sin ningún otro uso al dejar de necesitarse un id anónimo de dispositivo para esta feature.
- No se restringió por rol específico (`fan` vs `admin`/`referee`): en la práctica ya no hace falta, porque el guard de `router/index.js` (`publicTournamentRoutes`) ya redirige a admin/árbitro logueados fuera de `MatchDetail` hacia su propio panel — solo un aficionado (o un anónimo, ahora mandado al login) llega a ver esa página.

**Validado con:** backend + frontend locales. Como visitante anónimo, clic en 👍 → confirmado que redirige a `/login?redirect=%2FCOPACARIBE%2Fpartido%2F1571` sin llamar a la API. Se creó una cuenta `fan` de prueba directamente en la base (login por email/contraseña, ya que el login real de aficionados es por Google y no se puede probar en este entorno) — logueada, la misma reacción sí se registró y mostró "👍 1" en la UI. Confirmado también que el login por email/contraseña de staff (admin) no usa el parámetro `redirect` y siempre va a `/admin` — comportamiento preexistente del archivo, no modificado. Build de producción limpio. Usuario de prueba y reacciones de prueba eliminados de la base local al terminar.

### 2026-07-14 — Fix real: la sesión se cerraba sola ante cualquier fallo de red, no solo cuando el token era inválido
**Pedido:** "una vez logueado debe mantenerse asi hasta que se cierre sesion manualmente" — la sesión debía persistir hasta un logout explícito.

**Causa:** `verifySession()` en [src/stores/auth.js](src/stores/auth.js) se ejecuta una vez al arrancar la app ([src/main.js:24](src/main.js)) llamando a `GET /auth/me`. Su `catch` trataba **cualquier** error de esa llamada como "sesión inválida" y borraba el token — pero un `catch` de axios también se dispara por fallos que no tienen nada que ver con el token: red caída, timeout, o el backend "dormido" en Render/Neon (el propio proyecto tiene un keep-alive en `server.js` precisamente porque el backend se duerme). Bastaba abrir la app en un mal momento de conexión para que a alguien con un token perfectamente válido lo mandara al login sin razón.

**Corrección:** el `catch` ahora solo cierra la sesión si `err.response?.status === 401` (el servidor rechazó el token explícitamente, vía `authMiddleware`). Cualquier otro error deja el token/usuario intactos — la próxima vez que cargue la app se reintenta la verificación, sin haber perdido la sesión mientras tanto. El interceptor global de axios ([src/api/index.js](src/api/index.js)) ya seguía la misma regla (solo actúa en 401), así que ahora ambos coinciden.

**Validado con:** backend + frontend locales. Login real como admin, sesión confirmada activa. Se simuló un fallo de red puro (sin `response`, `code: 'ERR_NETWORK'`) en `/auth/me` y se llamó a `verifySession()` directamente — el token, usuario y `localStorage` quedaron intactos (`isLoggedIn` siguió en `true`). Se repitió simulando un 401 real — ahí sí se limpió la sesión correctamente, confirmando que el caso legítimo de token inválido sigue funcionando. Build de producción limpio.

### 2026-07-14 — Pantalla de "Sin conexión" para PWA y navegador
**Pedido:** el usuario proporcionó dos imágenes de diseño (`movil.png` retrato, `pc.png` horizontal, ambas en `~/Downloads`) para una pantalla de pantalla completa cuando no hay conexión a internet — "hay cosas que de plano no funcionan sin internet" (llamadas a la API, sockets). Pidió usar la imagen según el tipo de dispositivo y agregar el botón de reintentar debajo, donde mejor acomodara.

**Implementado:**
- Imágenes copiadas a [public/offline/movil.png](public/offline/movil.png) y [public/offline/pc.png](public/offline/pc.png) — al estar en `public/`, el `globPatterns: ['**/*.png', ...]` de `vite.config.js` ya las precachea automáticamente en el service worker (confirmado: el build pasó de 95 a 97 entradas precacheadas) — sin esto, la propia pantalla de "sin conexión" se rompería estando realmente sin conexión, porque la imagen no habría quedado disponible offline.
- [src/components/OfflineOverlay/OfflineOverlay.vue](src/components/OfflineOverlay/OfflineOverlay.vue) (nuevo) — overlay de pantalla completa (`position: fixed; inset: 0`) que escucha `window.addEventListener('online'/'offline', ...)` y `navigator.onLine`, y se muestra apenas se detecta que no hay red. Elige `movil.png` o `pc.png` según `window.innerWidth < 768` (reactivo a resize, por si se rota el dispositivo o se cambia el tamaño de ventana). El botón "Reintentar" se colocó debajo de la imagen (no encima — el pie de la imagen ya tiene gráficos decorativos de estadio donde un botón se vería mal) con un breve estado "Revisando..." antes de recargar la página si `navigator.onLine` ya es `true`; si sigue sin red, vuelve a "Reintentar" sin recargar.
- Montado globalmente en [src/App.vue](src/App.vue) (junto a `InstallPWA`) — cubre toda la app (público, admin, árbitro) tanto instalada como PWA o abierta como sitio normal, ya que el pedido fue "para tanto en pwa y online".

**Validado con:** build de producción (confirmado: precache subió de 95 a 97 entradas, +2.9MB por las dos imágenes). En navegador: se simuló `navigator.onLine = false` + evento `offline` — apareció el overlay correcto en desktop ancho (pc.png) y en viewport móvil (movil.png), con el botón "Reintentar" bien ubicado debajo en ambos casos. Clic en "Reintentar" mientras seguía offline → volvió a "Reintentar" sin recargar (confirmado que no se queda pegado en "Revisando..."). Se simuló `navigator.onLine = true` + evento `online` → la página recargó y el overlay desapareció.

### 2026-07-14 — Ajuste: la imagen de "Sin conexión" debe llenar toda la pantalla sin recortar el contenido
**Pedido:** "las imagenes deben verse correctamente y ocupar toda la pantalla" — la primera versión (imagen contenida con `object-contain` sobre fondo gris plano) dejaba franjas de color visibles a los lados; no llenaba de verdad la pantalla.

**Primer intento (descartado):** cambiar a `object-cover` (imagen estirada a 100% ancho/alto, recortando el sobrante). Al probarlo en un monitor ancho real (1920×1080, la resolución de escritorio más común) el recorte vertical empujaba el texto "Verifica tu conexión..." casi hasta el borde inferior, quedando apachurrado contra el botón "Reintentar" — en monitores ultra anchos (2560×1080) el problema se agravaba y hasta el logo superior se recortaba. Un degradado oscuro de refuerzo detrás del botón ayudaba pero no resolvía el recorte del contenido en sí, que es justo lo que pidió evitar ("verse correctamente").

**Solución final:** patrón de "fondo desenfocado + imagen íntegra al frente" (el mismo truco visual de Instagram Stories o Apple TV para rellenar pantalla con una imagen de otra proporción). Una copia de la misma imagen, ampliada (`scale-125`) y con `blur-2xl` + `brightness-[0.55]`, cubre el 100% de la pantalla de fondo (`bg-cover bg-center`) — así nunca se ven franjas de color plano, sin importar la proporción del monitor/dispositivo. Encima, la imagen original se muestra completa con `object-contain` (nunca recortada, logo/texto/personaje siempre íntegros), y el botón "Reintentar" va debajo de esa imagen contenida, en un espacio que ahora sí existe siempre porque la imagen nunca ocupa el 100% de la altura.

**Validado con:** build de producción limpio. Probado en 1920×1080 (el problema original), 2560×1080 (ultra ancho) y 375×812 (móvil) — en los tres casos la imagen se ve completa e íntegra, el fondo llena el 100% de la pantalla sin ninguna franja de color plano visible, y el botón "Reintentar" queda siempre en un espacio libre debajo, sin superponerse a ningún texto de la imagen.

### 2026-07-14 — Nuevas imágenes de "Sin conexión" con espacio dedicado para el botón
**Pedido:** el usuario diseñó dos imágenes nuevas ("ChatGPT Image 14 jul...") que sí incluyen una zona en blanco reservada bajo el texto — pidió usarlas y colocar el botón "Reintentar" dentro de ese espacio (no debajo, en flujo aparte), llenando igual el 100% de la pantalla en ambos casos.

**Cambios:**
- [public/offline/pc.png](public/offline/pc.png) (1536×1024) y [public/offline/movil.png](public/offline/movil.png) (864×1821 — mucho más alta que la versión anterior, con una banda vacía notablemente más grande bajo el texto) reemplazan a las imágenes previas, mismos nombres de archivo.
- [OfflineOverlay.vue](src/components/OfflineOverlay/OfflineOverlay.vue): el botón dejó de apilarse debajo de la imagen en un `flex-col` y ahora se posiciona `absolute` **dentro** de un contenedor que envuelve justo el tamaño renderizado de la imagen (`inline-block` + mismas restricciones `max-h` que el `<img>`, para que ambos midan exactamente lo mismo). El botón se ancla a `bottom: 8%` de esa caja — un valor que cae dentro de la zona vacía reservada en ambas imágenes (calculado a ojo sobre las capturas: ~8-9% desde abajo en la de escritorio, dentro de un margen mucho más amplio en la de mobile). El truco de fondo desenfocado (`blur-2xl` + `scale-125`) de la entrada anterior se mantiene igual, para seguir llenando el 100% de la pantalla sin franjas de color en cualquier proporción.

**Validado con:** build de producción limpio (precache subió a 97 entradas, tamaño total +69KB por el cambio de imágenes). Probado en 1920×1080, 2560×1080 (ultra ancho) y 375×812 (móvil) — en los tres casos el botón "Reintentar" cae limpiamente dentro del espacio en blanco de la imagen, sin superponerse al texto, y el fondo sigue llenando el 100% de la pantalla. Clic en "Reintentar" sin conexión → vuelve a "Reintentar" sin recargar, confirmando que el comportamiento no cambió, solo el posicionamiento visual.

### 2026-07-14 — La imagen (no un fondo simulado) debe llenar la pantalla, ahora que ya tiene espacio para el botón
**Pedido:** "ahora que existe espacio para el boton la imagen debe ocupar todo el espacio de la pantalla" — la entrada anterior seguía usando `object-contain` con `max-h-90vh`, así que la imagen real quedaba más chica que la pantalla (con el fondo borroso rellenando alrededor); el usuario pidió que fuera la imagen misma la que llenara el espacio, no un truco de fondo.

**Por qué no `object-fit: cover` directo:** se probó primero (matemática, sin necesidad de abrir el navegador) qué pasaría si la imagen de escritorio (1536×1024) cubriera un monitor 1920×1080 con `cover`: el recorte vertical necesario para igualar el ancho deja visible solo el rango Y=[80,944] de los 1024px originales — el botón, anclado al 8% inferior de la imagen COMPLETA (≈Y=942), queda a 2px del borde de ese recorte, es decir, se corta casi por completo en la resolución de escritorio más común que existe. En un monitor ultra ancho (2560×1080) el recorte es tan agresivo que tanto el logo de arriba como el texto/botón de abajo quedan completamente fuera del área visible. `object-cover` estaba descartado por buenas razones.

**Solución:** en vez de ajustar la imagen al ancho de la pantalla (lo que fuerza recorte vertical en monitores anchos), se ajusta siempre a la **altura** de la pantalla (`h-full`, `width: auto`) — la imagen nunca se recorta arriba ni abajo, sin importar cuán ancha sea la pantalla, porque el alto SIEMPRE coincide exactamente con el viewport. Cuando el monitor es más ancho que la proporción de la imagen (la mayoría de escritorios), sobra espacio a los lados, que rellena el mismo fondo borroso ya existente — nunca se ve como una franja de color plano, se ve como una continuación desenfocada de la propia imagen. En pantallas angostas (móvil), la imagen ya cubre el ancho completo por sí sola sin necesitar relleno.

**Validado con:** build de producción limpio. En el navegador, con `getBoundingClientRect()`: en 1920×1080 la imagen midió exactamente 1080px de alto (0 a 1080, sin recorte) y 1620px de ancho (dejando 150px de fondo borroso a cada lado); el botón quedó completamente dentro del viewport (top:949, bottom:993, muy por debajo de 1080). En 2560×1080 (ultra ancho) igual — toda la imagen visible, más margen borroso a los lados. En 375×812 (móvil) la imagen midió exactamente 375×812 — coincide pixel a pixel con la pantalla, sin ningún recorte ni margen. Botón de "Reintentar" verificado funcional en los tres casos.

### 2026-07-14 — El fondo desenfocado de los costados se veía grisáceo, no blanco
**Pedido:** el usuario mandó una captura mostrando el relleno de los costados (en resoluciones donde la pantalla es más ancha que la imagen) con un tono gris — pidió que fuera blanco, y que se revisara que el botón quedara bien ubicado sin tapar nada "en todos los casos y dispositivos".

**Causa:** el relleno lateral usaba una copia de la misma imagen ampliada (`scale-125`), desenfocada (`blur-2xl`) y oscurecida (`brightness-[0.55]`). El problema es que la imagen tiene franjas de navy oscuro arriba y abajo (el header/footer del diseño) — al difuminar una copia completa de la imagen, ese navy oscuro se mezcla con el fondo claro del centro, dando un gris turbio en vez de un blanco limpio, especialmente notorio en los bordes laterales donde debería verse solo el fondo claro de la imagen.

**Corrección:** se quitó el truco de fondo borroso por completo. El contenedor ahora usa `bg-white` liso — como el fondo real de ambas imágenes ya es blanco/gris muy claro, un blanco liso se funde con la imagen sin ninguna costura visible, y es más simple y confiable que intentar simular ese blanco con un desenfoque.

**Validado con:** build de producción limpio. Se armó un script de verificación (`getBoundingClientRect` sobre la imagen y el botón) y se corrió en 8 resoluciones distintas: 1920×1080, 2560×1080 (ultra ancho), 1366×768 (laptop), 1024×600 (laptop chico), 812×375 (celular en horizontal), 768×1024 y 767×1024 (a cada lado del punto de quiebre móvil/escritorio, confirmando que cada uno carga la imagen correcta) y 390×844 (celular). En las 8, `botón completamente dentro de la imagen` y `botón completamente dentro de la pantalla` dieron verdadero — ninguna tapa texto ni se sale de la pantalla. Captura visual adicional en 812×375 confirmando que se ve limpio incluso en el caso más chico probado.

### 2026-07-14 — Fix real: el botón sí tapaba el texto en resoluciones típicas de laptop — la verificación anterior no lo detectaba
**Pedido:** el usuario mandó una captura de 1366×768 (resolución de laptop muy común) mostrando el botón "Reintentar" literalmente encima de "Verifica tu conexión e inténtalo de nuevo." — pidió revisar y acomodar bien el botón en todas las resoluciones sin taparlo con texto ni nada más.

**Por qué la verificación anterior no lo detectó:** el script de la entrada previa solo comprobaba que el botón estuviera *dentro de los límites de la imagen* y *dentro de la pantalla* — eso siempre da verdadero sin importar dónde caiga el botón dentro de la imagen. Nunca comparó la posición del botón contra dónde está *el texto real*, que está dibujado como píxeles fijos dentro del PNG (no es HTML medible por DOM). Ese hueco en la prueba es lo que dejó pasar el problema.

**Medición real del texto (Python/Pillow, sobre los archivos en `public/offline/`):** se recortó la franja inferior de cada imagen con una regla de referencia dibujada cada 2% de altura, y se buscó la última fila con píxeles oscuros (texto) en la franja central. Resultado: en `pc.png` el párrafo termina a ~11% desde abajo (mucho menos margen del que se asumió a ojo la vez anterior); en `movil.png` termina a ~27% desde abajo (con mucho más margen de sobra). Como las dos imágenes no comparten ni proporción ni la posición del texto, un mismo `bottom: 8%` para ambas — lo que había antes — dejaba el botón demasiado arriba en la de escritorio, invadiendo el final del párrafo apenas la pantalla no fuera muy alta (1366×768 tiene menos altura que 1920×1080, y el botón mide lo mismo en píxeles pero ocupa más % de una imagen más chica).

**Corrección:** [OfflineOverlay.vue](src/components/OfflineOverlay/OfflineOverlay.vue) ahora usa un `buttonBottomPct` computado que depende de qué imagen está activa: `3%` para `pc.png` (deja el borde superior del botón en el peor caso alrededor de 8-9% desde abajo, con margen bajo el 11% donde termina el texto) y `8%` para `movil.png` (que ya tenía mucho margen de sobra, sin necesidad de tocarlo).

**Validado con:** build de producción limpio. Re-verificado en las mismas 8 resoluciones de la entrada anterior más un caso extra (375×600, altura corta de celular) — en 1366×768 específicamente (la resolución de la captura del usuario) se confirmó por captura de pantalla que el botón ya no toca ninguna línea de texto, y por `getBoundingClientRect` que su borde superior queda a 8.73% desde abajo de la imagen (antes invadía por encima del ~11% donde termina el párrafo). Botón de "Reintentar" verificado funcional tras el cambio.

### 2026-07-14 — Dos bugs más al pedir un ajuste final "en todas las resoluciones": distorsión de imagen + botón que volvía a fallar en ventanas cortas
**Pedido:** "ajusta el boton en todas la resoliciones para que se vea bien en todos los casos" — pasada final de verificación exhaustiva pedida explícitamente por el usuario tras el fix anterior.

**Bug 1 — la imagen se distorsionaba (se estiraba) en ventanas angostas y altas:** probando 800×1200 (una ventana de escritorio inusualmente alta y angosta, pero real) apareció el personaje/logo visiblemente estirado — la proporción renderizada era 0.662 en vez de la real 1.5. Causa: el preflight de Tailwind aplica `max-width: 100%` a todo `<img>` por defecto; mis clases (`h-full w-auto`) nunca lo anulaban, así que ese `max-width` competía con `height:100%`, y sin `object-fit` explícito (el valor por defecto es `fill`, que sí distorsiona) el navegador estiraba la imagen para llenar la caja resultante de ese conflicto en vez de respetar su proporción real. Corrección: se agregó `max-w-none object-contain` al `<img>` — `max-w-none` anula el preflight, `object-contain` es un seguro adicional para que nunca se distorsione aunque algún otro estilo vuelva a imponer un tamaño de caja incompatible con la proporción real.

**Bug 2 — el botón volvía a montarse sobre el texto, pero solo en ventanas cortas:** el `bottom` fijo en % (3% para pc.png) solo es seguro para el rango de alturas que se probó la vez anterior. Como el botón mide lo mismo en píxeles sin importar cuánto mida la ventana (su alto sale de padding/tipografía fijos, no de unidades `vh`), en una ventana más baja el botón "pesa" más como porcentaje de una imagen más chica — a 1024×600 quedaba a 1.3 puntos de tocar el texto por pura casualidad, y a 1024×500 ya lo tocaba. Un valor fijo de `bottom` no podía funcionar para todas las alturas a la vez.

**Corrección:** se reemplazó el `bottom` fijo por un cálculo dinámico (`buttonBottomPct`, reactivo a resize). En vez de fijar dónde ancla el botón, se fija dónde debe quedar **su borde superior** (`SAFE_TOP_PCT`: 13% para pc.png, 30% para movil.png — con margen de sobra sobre el ~11%/~27% donde termina el texto real, medido a pixel) y se despeja el `bottom` necesario restando la altura real del botón (medida con un `ref` + `getBoundingClientRect()`, no asumida). Así el borde superior del botón queda fijo en ese % sin importar cuánto mida la ventana, porque la fórmula compensa el alto fijo del botón contra el alto variable de la imagen en cada resize.

**Validado con:** build de producción limpio. Se repitió la batería de resoluciones de la entrada anterior más los casos que expusieron ambos bugs: 800×1200 (proporción renderizada confirmada en 1.500, igual a la real, sin distorsión) y 1024×500 / 1024×600 (borde superior del botón confirmado en exactamente 13.00% desde abajo en ambas, en vez de variar y arriesgar solaparse). Se repitió también en 1920×1080, 2560×1080, 390×844 y 375×400 (celular muy bajito) — el borde superior del botón dio siempre 13.00% (pc.png) o 30.00% (movil.png), sin excepción. Botón de "Reintentar" verificado funcional al final.

### 2026-07-14 — Fix: el contador de seguidores sumaba 2 en lugar de 1 para la misma persona ("seguidores falsos")
**Pedido:** el usuario reportó (en tono urgente) que el contador de seguidores a veces suma 2 en vez de 1, y que llegó a ver "seguidores falsos".

**Causa raíz encontrada:** el conteo deduplicado en [backend/utils/followers.js](backend/utils/followers.js) reconoce que un mismo seguidor puede tener fila en la tabla por-dispositivo (`team_follows`/etc., por `endpoint`) y en la tabla por-cuenta (`user_team_follows`/etc., por `user_id`) a la vez, y usa `push_subscriptions.user_id` para saber que ambas filas son la misma persona. Pero `push_subscriptions.user_id` solo se llena cuando se llama `POST /push/subscribe` **con sesión activa**, y ese endpoint solo se dispara al activar notificaciones por primera vez o al re-tocar el botón manualmente (ver `src/composables/usePWA.js`) — nunca se vuelve a llamar solo por loguearse. Resultado: cualquier persona que (1) activó notificaciones estando anónima y (2) después inició sesión y siguió un equipo/torneo/jugador, quedaba con una fila "anónima" (endpoint) y otra "de cuenta" (user_id) sin ningún vínculo conocido entre ambas — la query las contaba como 2 identidades distintas en vez de 1.

**Corrección:**
- Nueva tabla `device_accounts` (`endpoint` PK, `user_id`) en [backend/config/db-schema.js](backend/config/db-schema.js) y [backend/config/db-sqlite-init.js](backend/config/db-sqlite-init.js) — vínculo dispositivo↔cuenta independiente de si hay push activado.
- Nuevo helper `linkDeviceAccount(endpoint, userId)` en [backend/utils/followers.js](backend/utils/followers.js), llamado desde las 6 rutas de follows en [backend/routes/index.js](backend/routes/index.js) (`/follows`, `/follows/add`, `/follows/tournaments`, `/follows/tournament/add`, `/follows/players`, `/follows/player/add`) cada vez que la request trae a la vez `endpoint` y sesión válida — cubre tanto el sync automático al arrancar la app (`main.js` → `syncFromServer`, que corre en cada carga con sesión) como cualquier follow/unfollow hecho estando logueado.
- Las queries de conteo (`followerCountFactory`/`followerCountsFactory`) ahora hacen `LEFT JOIN device_accounts` además de `push_subscriptions`, y prefieren `device_accounts.user_id` en el `COALESCE` de resolución de identidad.
- Como el conteo se calcula en vivo (no hay cache ni columna de contador guardada), los conteos ya inflados en producción se autocorrigen solos apenas cada dispositivo afectado vuelva a sincronizar (que pasa automáticamente en cada carga de la app con sesión activa) — no hace falta backfill manual.

**Validado con:** reproducción exacta del bug en SQLite local — se insertó una fila en `team_follows` por `endpoint` y otra en `user_team_follows` con el mismo `user_id`, sin vínculo previo: `teamFollowerCount()` devolvía 2. Tras llamar `linkDeviceAccount(endpoint, userId)` (simulando el sync), el mismo conteo bajó a 1. Repetido a nivel HTTP real contra el servidor local (`POST /api/follows` con `endpoint` + JWT válido) confirmando que la ruta crea la fila en `device_accounts` correctamente. Datos de prueba eliminados tras la verificación.

### 2026-07-14 — INCIDENTE: el fix anterior tumbó toda la información de torneos en producción
**Reporte del usuario:** "desaparecio la informacion des torneo en produccion la afficion no puede ver nada" — afectaba casi todas las vistas públicas.

**Causa raíz:** al agregar la tabla `device_accounts` (fix anterior), se puso dentro de `PG_SCHEMA` en [backend/config/db-schema.js](backend/config/db-schema.js) — ese arreglo **solo se ejecuta cuando la base de datos es nueva** (`config/db.js` la corre solo si la tabla `users` todavía no existe). En producción `users` ya existía desde hace meses, así que `device_accounts` nunca se creó ahí. Pero `backend/utils/followers.js` ya hacía `LEFT JOIN device_accounts` sin condición en cada conteo de seguidores — cualquier endpoint que calculara `followerCount` (torneo, equipo, jugador, listados admin) tiraba `relation "device_accounts" does not exist` y, al no estar atrapado, el error subía hasta el error handler global y devolvía 500 para la petición completa — no solo el contador, toda la vista.

**Corrección (doble, para que no vuelva a pasar):**
- Se movió la creación de `device_accounts` de `PG_SCHEMA` a `PG_MIGRATIONS` en [backend/config/db-schema.js](backend/config/db-schema.js) — ese arreglo corre en *cada* arranque del servidor sin importar si la base ya existía, igual que otras tablas agregadas después del lanzamiento inicial (`tournament_follows`, `user_team_follows`, etc., que ya seguían ese patrón correctamente).
- Se envolvieron las consultas de `followerCountFactory`/`followerCountsFactory` en [backend/utils/followers.js](backend/utils/followers.js) en try/catch — si el conteo de seguidores falla por cualquier motivo (tabla faltante, timeout, lo que sea), ahora degrada a `0`/mapa vacío en vez de tumbar la respuesta completa. Una función accesoria (contador social) nunca debe poder romper la información real del torneo.

**Validado con:** en SQLite local se renombró temporalmente `device_accounts` para simular la tabla ausente — antes del fix esto habría devuelto 500; con el fix, `GET /tournaments/:slug` respondió 200 con toda la información del torneo intacta y `followerCount: 0`. Tabla restaurada después de la prueba.

**Lección aprendida:** cualquier tabla nueva agregada después del lanzamiento inicial debe ir en `PG_MIGRATIONS`, nunca en `PG_SCHEMA` — ese arreglo es exclusivo para instalaciones desde cero y no vuelve a correr jamás en una base ya existente.

**Refuerzo adicional (mismo día):** tras el deploy del fix anterior, el usuario reportó una tanda breve de 500 en `/api/tournaments`, `/api/teams` y las rutas de `/api/follows/*` — coincidía con el reinicio del servicio en Railway (log del socket mostraba "Desconectado"/"Conectado"), y para cuando se verificó de nuevo todo respondía 200. Aun así, se detectó que `linkDeviceAccount()` se llamaba en las 6 rutas de follows sin try/catch propio — a diferencia de `followerCountFactory`/`followerCountsFactory`, que ya quedaron blindados. Se envolvió también `linkDeviceAccount()` en try/catch (dentro de [backend/utils/followers.js](backend/utils/followers.js)) para que un fallo ahí nunca pueda tumbar una petición de follows completa. Regla general aplicada: ninguna función accesoria/social debe poder propagar su error hacia una respuesta que también carga información real de la plataforma.

### 2026-07-15 — Feature: soportar 2+ equipos del mismo club en la misma categoría, en una sola inscripción
**Pedido:** "si quiero inscribir dos equipos del mismo club y la misma categoria una especie de equipo a y b, la plataforma debe permitirlo desde la inscripcion hasta el admin y mostarlo en todos los roles."

**Diagnóstico previo:** a nivel de tabla `teams` esto ya funcionaba (el índice único es por `tournament_id+category_id+nombre`, dos nombres distintos en la misma categoría nunca chocan). El problema real era que **todo el pipeline de inscripción asumía 1 equipo por (inscripción, categoría)**: `categories_json` solo guardaba `{id,name}` (un nombre de equipo por inscripción completa), `createTeamsFromInscription` creaba un único equipo por categoría, y las rutas de jugadores (`POST/PUT .../players`) buscaban el equipo real solo por `(inscription_id, category_id)` — sin forma de distinguir un segundo equipo en la misma categoría.

**Diseño implementado:**
- `categories_json` ahora guarda `{id,name,teamName}` por entrada — el mismo `id` de categoría puede repetirse con un `teamName` distinto para representar un segundo (o tercer) equipo. Nuevo campo opcional `extraTeams: {[categoryId]: string[]}` en `POST /inscriptions` ([backend/routes/index.js](backend/routes/index.js)).
- Nueva columna `inscription_players.team_name` (con backfill de filas viejas, ver [backend/config/db-schema.js](backend/config/db-schema.js)/[db-sqlite-init.js](backend/config/db-sqlite-init.js)) para saber a cuál de los equipos de la categoría pertenece cada jugador propuesto.
- `createTeamsFromInscription` crea un equipo real por cada `(categoría, teamName)` en vez de uno por categoría — y si el nombre choca con un equipo de OTRA inscripción (dos solicitudes distintas llegaron al mismo nombre antes de aprobarse), ya no se traga el error en silencio: lo reporta como fallo real en vez de dejar jugadores huérfanos sin equipo.
- Cupo de jugadores (`max_players_per_team`), el límite de "2 niñas con excepción de edad" y el chequeo de dorsal duplicado ahora se escopan por equipo (no solo por categoría) — bug que esta feature hubiera introducido si esos tres checks se hubieran dejado como estaban (compartirían cupo/límite/dorsales entre el equipo A y B de la misma categoría). El índice único de dorsal (`uq_inscplayers_insc_cat_number` → renombrado `uq_inscplayers_insc_cat_team_number`) se actualizó para incluir `team_name`.
- El chequeo de CURP duplicada **no cambia** — se queda escopado solo por categoría, a propósito: una persona no debe poder jugar para el equipo A y el B del mismo club a la vez (anti-cachirul).
- `src/pages/Inscription.vue`: bajo cada categoría marcada aparece "+ Agregar otro equipo en esta categoría".
- `src/pages/InscriptionPlayers.vue`: sub-tabs de equipo cuando una categoría tiene 2+, con estado (jugadores en captura, errores, etc.) reindexado por clave compuesta `categoryId::teamName` — necesario porque antes todo ese estado estaba indexado solo por `categoryId`, lo que habría cruzado los formularios del equipo A y B entre sí.
- `src/pages/admin/Inscriptions.vue`: las tarjetas y el modal de detalle muestran cada equipo distinguible ("Sub-7 — Club X B"), con los jugadores agrupados por equipo dentro de la misma categoría. Los responsables se quedan compartidos entre los equipos de una categoría (decisión de alcance, no bug, documentada en la UI).

**Validado con:** flujo real de punta a punta en navegador — inscripción pública de "Club Robusto A" + "Club Robusto B" en Sub-7 desde un solo formulario; chequeo de nombre duplicado confirmado (rechazó reinscribir "Club Robusto A" ya existente); aprobación desde admin creó los 2 equipos reales; tarjeta y modal del admin mostraron ambos equipos distinguibles; registro de jugadores con sub-tabs funcionando, mismo dorsal #10 aceptado en ambos equipos sin choque; misma CURP entre equipo A y B correctamente bloqueada ("ya está registrada en esta categoría"); ambos equipos visibles de forma independiente en la vista pública del torneo y en su perfil individual, cada uno con su propio jugador. Build de producción del frontend limpio. Datos de prueba eliminados y configuración local revertida al terminar.

### 2026-07-15 — Fix: subida de archivos en inscripciones rechazaba tipos de archivo válidos
**Pedido:** "haz mas robusto el tema de las inscripciones en cuanto a archivos que permita todo tipo de archivos para evitar errores."

**Causa raíz:** el `fileFilter` de Multer en `backend/routes/index.js` tenía una lista blanca de 14 extensiones (`.jpg,.jpeg,.png,.webp,.gif,.svg,.bmp,.tiff,.tif,.heic,.heif,.mp4,.mov,.pdf`) — cualquier archivo fuera de esa lista se rechazaba en silencio (`cb(null, false)`) con un error genérico "Archivo no válido". Pasaba con documentos oficiales escaneados en formatos menos comunes o exportados por apps distintas al carrete del teléfono. El `accept` de los inputs del frontend es solo un filtro cosmético del selector de archivos del sistema operativo — nunca era la causa real, el bloqueo estaba 100% en el backend.

**Corrección:**
- Se quitó el `fileFilter` de Multer por completo — ahora solo se valida el tamaño (25 MB, sin cambios).
- `uploadToCloudinary` ahora decide `resource_type` correctamente para CUALQUIER archivo: `image` solo si el mimetype empieza con `image/` o es `.heic`/`.heif` (mimetype ambiguo en iPhone), `video` si empieza con `video/`, y `raw` para todo lo demás (PDF, Word, Excel, ZIP, lo que sea) — antes cualquier archivo que no fuera video o PDF se mandaba como `image` por defecto, lo cual Cloudinary habría rechazado en cuanto se quitara la lista blanca que hasta ahora lo prevenía indirectamente.
- Se quitó el `accept="image/*,.heic,.heif,application/pdf"` de los inputs de "documento oficial" en `src/pages/InscriptionPlayers.vue` (alta y edición) — ahora el selector de archivos no limita el tipo, para credenciales/actas en formatos menos comunes. Los campos de foto (logo, jugador, responsable) se quedan enfocados en imágenes a propósito, tiene sentido para ese campo y evita subir por error un archivo que rompería la vista previa `<img>`.
- Se agregó `console.error` al catch de `POST /upload/public` (antes tragaba el error en silencio sin loguearlo, a diferencia de `POST /upload`) — ayudó a diagnosticar este mismo fix y evita que un futuro problema de Cloudinary quede invisible.

**Validado con:** subida real contra la cuenta de Cloudinary de producción vía `curl` — un `.txt` y un `.xyz` (extensión desconocida) que antes se rechazaban ahora suben correctamente como `resource_type: raw`; un `.png` y un `.pdf` de control confirmaron que el comportamiento para imágenes y PDF no cambió (`image` y `raw` respectivamente, igual que antes). Build de producción del frontend limpio.

### 2026-07-15 — INCIDENTE: la feature de equipos A/B tumbó el registro normal (2+ categorías, mismo nombre)
**Reporte del usuario:** captura de pantalla mostrando "El nombre de equipo "Venados FC Cancun" está repetido en tu propia solicitud" al intentar una inscripción normal.

**Causa raíz:** el chequeo de "nombre repetido dentro del mismo envío" (agregado junto con la feature de equipos A/B, ver entrada anterior) comparaba el `teamName` de cada entrada de `categories` **sin tener en cuenta la categoría**. Pero es *completamente normal e intencional* que el mismo `team_name` aparezca una vez por cada categoría seleccionada (así ha funcionado siempre inscribirse en varias categorías a la vez, ej. un equipo en Sub-7 Y Sub-9) — el check lo interpretaba como un duplicado real y bloqueaba con 400 cualquier inscripción con 2+ categorías, que es el caso más común de la plataforma.

**Corrección:** el chequeo ahora escopa la detección de duplicados por `categoryId` (`seenByCategory[cat.id]` en vez de un único `Set` global en `backend/routes/index.js`) — solo bloquea si el MISMO nombre se repite dentro de la MISMA categoría (el caso real que se quería prevenir: escribir "Club X A" dos veces como equipo extra), nunca entre categorías distintas.

**Validado con:** reproducido el bug exacto en local (2 categorías, mismo `team_name`, sin equipos extra) — antes del fix devolvía 400, después crea la inscripción correctamente con una entrada por categoría. Se confirmó que el caso que sí debe bloquearse (mismo nombre repetido como equipo extra dentro de la MISMA categoría) sigue bloqueado. Build de producción limpio.

**Nota de operación:** al diagnosticar este bug se hicieron pruebas directas contra la API de producción para confirmar el mensaje de error exacto — una de ellas (`auto_approve_inscriptions` activo en ese torneo) creó un equipo real de prueba ("Diagnostico Test XYZ", categoría Sub-7). Se le indicó al usuario eliminarlo manualmente desde Admin → Equipos, ya que no se cuenta con credenciales de admin de producción para hacerlo automáticamente.

### 2026-07-15 — Mejora: nombre del "Equipo 1" editable + contraste de texto en equipos extra
**Pedido:** captura mostrando el texto del input de "otro equipo" apenas visible (sin color explícito), más "si agrego un equipo más a cualquier categoría debo poder editar los nombres de ambos equipos".

**Cambios en `src/pages/Inscription.vue`:**
- Se agregó `text-slate-900` explícito a los inputs de equipo extra — antes no tenían clase de color y heredaban un tono muy claro, casi ilegible.
- Nuevo campo `form.primaryTeamNames` (`{ [categoryId]: string }`): en cuanto una categoría tiene un 2do equipo, aparece un input editable para el "Equipo 1" (precargado con el nombre global del equipo, pero ajustable de forma independiente por categoría) junto a los inputs de "Equipo 2, 3...". Antes el nombre del primer equipo dependía únicamente del campo global "Nombre del equipo" de más abajo, sin poder afinarlo por categoría.
- `backend/routes/index.js`: `POST /inscriptions` acepta el nuevo campo opcional `primaryTeamNames` y lo usa como override del nombre principal de esa categoría (si no se manda, sigue usando el nombre global de siempre — sin cambio de comportamiento para inscripciones de un solo equipo).

**Validado con:** flujo real en navegador — al agregar un 2do equipo a Sub-7, el campo "Equipo 1" aparece precargado y editable, el texto se ve en negro con buen contraste; se guardaron "Sporting Club A" y "Sporting Club B" (dos nombres distintos y editados) en `categories_json`, confirmado directo en la base de datos. Build de producción limpio.

### 2026-07-15 — Fix: overflow horizontal en móvil (stats de equipo, listados de torneos)
**Pedido:** capturas mostrando el contador de un equipo perdiendo el texto "SEGUIDORES" en móvil, y la sección "Los más seguidos" con tarjetas cortadas en el borde derecho.

**Causa raíz #1 (`TeamDetail.vue`):** la fila de stats (Jugadores/Partidos/Seguidores) usa `grid grid-cols-2 sm:grid-cols-5` — las celdas de DT/Capitán ya tenían `min-w-0` + `truncate`, pero Jugadores/Partidos/Seguidores no, así que esas 3 celdas no podían encogerse por debajo del ancho de su contenido y forzaban overflow horizontal en el layout de 2 columnas de móvil.

**Causa raíz #2 (patrón más profundo, encontrado al reproducir "Los más seguidos"):** agregar `min-w-0`+`truncate` a los elementos INTERNOS de una tarjeta no basta si la tarjeta misma es hija directa de un `grid` sin `min-w-0` — un elemento grid/flex tiene `min-width: auto` por defecto, así que si su contenido interno "quiere" ser más ancho, la tarjeta completa (y por lo tanto la pista del grid) crece más allá del contenedor aunque el texto de adentro tenga `truncate`. Se confirmó inyectando un nombre largo real ("Cañeros de Zacatepec Filial Valladolid...") vía DOM: con solo el fix interno, la tarjeta seguía saliéndose del viewport (`cardWidth: 533px` vs `gridParentWidth: 343px`); con `min-w-0` en la tarjeta misma, truncó correctamente.

**Corrección:**
- `TeamDetail.vue`: `min-w-0` + `truncate` agregado a las celdas de Jugadores/Partidos/Seguidores (mismo patrón que DT/Capitán). Grid de Victorias/Empates/Derrotas/Goles y el grid de la Plantilla pasan a `grid-cols-2 sm:grid-cols-4` / con `min-w-0` en cada tarjeta de jugador, para no depender de que 4-5 columnas quepan en pantallas angostas.
- `PlayerDetail.vue`: mismo ajuste `grid-cols-2 sm:grid-cols-4` en el grid de stats (Goles/Asistencias/Amarillas/Rojas).
- `Home.vue` y `Tournaments.vue`: se agregó `min-w-0` a las tarjetas de torneo (`router-link.card`, hijas directas del grid) y `min-w-0`+`truncate` a su nombre/ubicación interna — mismo patrón en ambos archivos porque comparten el mismo diseño de tarjeta.

**Validado con:** en navegador, a 320px/375px de ancho — confirmado `document.documentElement.scrollWidth === clientWidth` (sin overflow) en `TeamDetail.vue` con datos reales, y en `Home.vue`/tarjetas de torneo tras inyectar nombres largos vía DOM para forzar el escenario exacto de las capturas del usuario — el texto ahora trunca con elipsis en vez de desbordar. Revisado también el dashboard de admin en 375px (ya manejaba esto correctamente, sin cambios necesarios). Build de producción limpio.

### 2026-07-15 — Fix: overflow horizontal en móvil de los 4 contadores del hero público (`TournamentHome.vue`)
**Pedido:** captura de la página pública de un torneo ("COPA CARIBE") mostrando "104 JUGADORES", "0 PARTIDOS" y "SEG" (Seguidores cortado), con el número de Equipos tampoco visible — "corrigelo haz responsivo esos contadores de los 4".

**Causa raíz:** la fila de stats (Equipos/Jugadores/Partidos/Seguidores) era un `flex items-center justify-center gap-8 md:gap-16` **sin `flex-wrap`**, con separadores verticales entre cada stat y números en `text-4xl md:text-6xl` (36-60px). Con los 4 contadores visibles a la vez (torneo con jugadores Y seguidores registrados) más 3 separadores, el ancho total de la fila supera con facilidad los ~375-414px de un viewport móvil típico; al no poder envolver (`flex` sin wrap) y estar centrada (`justify-center`) dentro de una sección con `overflow-hidden`, el navegador simplemente cortaba los extremos (Equipos a la izquierda, Seguidores a la derecha), dejando visibles solo los 2 stats del medio — coincide exactamente con la captura del usuario.

**Corrección en `src/pages/TournamentHome.vue`:**
- Se agregó `flex-wrap` al contenedor de la fila — si los 4 stats no caben en una sola línea, envuelven a 2 filas de 2 en vez de desbordar.
- Tamaño de número escalonado en 3 pasos en vez de 2: `text-3xl sm:text-4xl md:text-6xl` (antes saltaba directo de `text-4xl` a `md:text-6xl` sin paso intermedio para pantallas pequeñas).
- Gap reducido en móvil: `gap-x-6 gap-y-3 sm:gap-x-8 md:gap-x-16` (antes `gap-8` fijo incluso en el viewport más angosto).
- Los separadores verticales (`w-px h-12 bg-white/20`) se ocultan en móvil (`hidden sm:block`) — son decorativos, consumían ancho innecesario y se verían fuera de lugar entre dos filas cuando el layout envuelve.

**Validado con:** en navegador, sobre el torneo real "COPA CARIBE" (`/COPACARIBE`) — a 375px con sus 3 stats reales (11 Equipos, 2 Jugadores, 25 Partidos) no hay overflow (`scrollWidth === clientWidth`). Para reproducir el caso exacto de la captura (4to contador de Seguidores > 0), se clonó vía DOM un 4to bloque de stat con 128 seguidores: a 375px los 4 caben en una sola fila, totalmente legibles; a 320px con un valor de 4 dígitos (1284) la fila envuelve limpiamente a 2x2 sin cortar ningún número, en ambos casos `scrollWidth === clientWidth`. Confirmado en desktop que el diseño original (separadores visibles, tamaños grandes) no cambió. Build de producción limpio.

### 2026-07-15 — Fix: botón "Seguir" se sobreponía al nombre en `PlayerDetail.vue` + feature: notificar goles/asistencias/tarjetas a quien sigue al jugador
**Pedido:** captura de la ficha pública de un jugador ("Jared Alejandro Lagunas Chan") mostrando el botón de corazón (seguir) montado encima de la tercera línea del nombre, más "checa lo responsivo, además revisa que cuando haya goles asistencias etc. del jugador que sigas notifique".

**Causa raíz del overlap:** el contenedor de la ficha (`class="card flex items-center gap-6"`) usaba `items-center`. Con un nombre corto esto no se nota, pero en cuanto el nombre envuelve a 2+ líneas (`h1` sin límite de ancho ni `truncate`), la fila completa crece en altura y `items-center` centra TODOS los hijos (foto, columna de texto y botón) respecto a esa altura total — el botón, al ser más chico que el bloque de texto, terminaba flotando a la mitad de la fila, justo encima de una línea intermedia del nombre. Reproducido en local incluso con "Samuel Gutierrez" (nombre corto, 2 líneas) y confirmado que empeora con el nombre largo real de la captura (4 líneas).

**Corrección en `src/pages/PlayerDetail.vue`:** contenedor cambiado a `items-start` (foto, texto y botón alineados arriba, no centrados) — así el botón se queda fijo en la esquina superior sin importar cuántas líneas ocupe el nombre. De paso: foto reducida en mobile (`w-20 h-20 md:w-24 md:h-24`), nombre con `break-words` en vez de desbordar, nombre del equipo con `truncate`, y paddings del botón ajustados para pantallas chicas.

**Feature de notificaciones (`backend/routes/index.js`):** ya existían las tablas `player_follows`/`user_player_follows` y los endpoints para seguir/dejar de seguir jugadores, pero **no existía ningún envío de push a los seguidores de un jugador** — a diferencia de `sendPushToTeams`/`sendPushToTournaments` (equipos/torneos), no había un `sendPushToPlayers`. Se agregó:
- `sendPushToPlayers(playerIds, payload)` — mismo patrón que `sendPushToTeams`, resolviendo tanto follows por dispositivo (`player_follows`) como por cuenta logueada (`user_player_follows`), expuesta como `global.sendPushToPlayers`.
- En `POST /matches/:id/events` (donde el árbitro registra gol/asistencia/tarjeta), tras guardar el evento se dispara `sendPushToPlayers([playerId], ...)` con mensaje personalizado por tipo: "⚽ ¡Gol!", "🎯 ¡Asistencia!", "🟨 Tarjeta amarilla", "🟥 Tarjeta roja". El autogol se excluye a propósito — no suma a las estadísticas personales del jugador ni es un logro para celebrar.
- Se agregó el JOIN a `tournaments` en la consulta de `updatedMatch` de esa misma ruta (antes solo tenía equipos) para poder armar la URL de destino de la notificación (`/{slug}/partidos`) — el resto de consumidores de `updatedMatch` (socket) no se ven afectados, es un campo adicional.

**Validado con:** en navegador a 375px — reproducido el overlap exacto con el nombre real de la captura inyectado vía DOM, confirmado que con `items-start` el botón queda perfectamente arriba sin tocar ninguna línea del nombre, en ningún ancho de prueba (320px/375px) hay overflow horizontal. Para la notificación: registrada una suscripción push de prueba + seguido el jugador real "Samuel Gutierrez" (id 134) con ese dispositivo, puesto un partido real en estado `live` temporalmente, y creado un evento de gol real vía `POST /matches/:id/events` con las credenciales de admin — confirmado en los logs del servidor que `sendPushToPlayers` resolvió exactamente 1 endpoint (el de prueba) y armó el payload correcto (`⚽ ¡Gol! / Samuel Gutierrez anotó un gol / /COPACARIBE/partidos`). Limpiados todos los datos de prueba al terminar (suscripción, follow, y el propio evento) y revertido el partido a su estado original.

**Nota de operación:** al intentar borrar el evento de prueba vía `DELETE /match-events/:id` se descubrió un bug preexistente y no relacionado: la reversión de stats usa `GREATEST(0, col-1)`, función exclusiva de PostgreSQL que no existe en SQLite — en el entorno local (SQLite) esto tumbó ese borrado a medias (el marcador del partido sí se revirtió, pero el contador de goles del jugador y la fila del evento quedaron huérfanos) y tuve que limpiarlo manualmente por SQL directo. No afecta producción (usa Postgres), pero sí rompe el borrado de eventos para cualquiera que desarrolle localmente con SQLite. Corregido enseguida (ver siguiente entrada).

### 2026-07-15 — Fix: `GREATEST()` no existe en SQLite, tumbaba el borrado de eventos de partido en desarrollo local
**Causa raíz:** las 3 rutas que revierten estadísticas de jugador al borrar/deshacer un evento de gol/asistencia/tarjeta (`DELETE /match-events/:id`, borrado de un partido finalizado, y borrado de una fase completa con partidos finalizados — `backend/routes/index.js` líneas ~274-277, ~873-876 y ~1040-1044) usaban `UPDATE players SET goals=GREATEST(0,goals-1) WHERE id=$1`. `GREATEST()` es una función exclusiva de PostgreSQL; en SQLite (usado en desarrollo local) la consulta truena con `SqliteError: no such function: GREATEST`, y como no hay transacción envolviendo el handler, el error a medio camino dejaba el marcador del partido ya revertido pero el contador del jugador y la fila del evento como huérfanos — se descubrió exactamente así mientras se verificaba la feature de notificaciones de jugadores (entrada anterior).

**Corrección:** las 12 ocurrencias (3 ubicaciones × 4 stats: goles, asistencias, amarillas, rojas) se cambiaron a `CASE WHEN col>0 THEN col-1 ELSE 0 END`, sintaxis SQL estándar soportada igual por PostgreSQL y SQLite — sin cambio de comportamiento en producción (Postgres), corrige el desarrollo local (SQLite).

**Validado con:** en local (SQLite) — partido real puesto en `live` temporalmente, se creó y borró un evento de cada tipo (gol, asistencia, amarilla, roja) para el jugador real "Samuel Gutierrez" (id 134) vía `POST`/`DELETE /matches/:id/events` con credenciales de admin. Los 4 casos: sin error, marcador y contadores revertidos correctamente, fila del evento sí eliminada (antes del fix, el gol dejaba el contador y la fila huérfanos). Estado final del jugador y del partido verificado idéntico al original antes de la prueba. `node --check` limpio. Build de producción del frontend sin cambios relevantes (fix es solo backend).

### 2026-07-15 — Fix: seguir a un jugador no exigía login (sí lo exigen equipos y torneos)
**Pedido:** "recuerda que no puedes seguir ni jugadores ni equipos ni nada sin antes loguearte y debe redirigir al login si no lo estás" — más la aclaración de que cerrar sesión debe ser siempre una acción manual del usuario, nunca automática.

**Causa raíz:** el requisito de login para seguir YA estaba implementado correctamente en `TeamDetail.vue` y `TournamentHome.vue` (ambos con el mismo patrón: si `!auth.isLoggedIn`, `router.push({ name: 'Login', query: { redirect: route.fullPath } })` y no llaman a la API) — pero `PlayerDetail.vue` se agregó después (sesión anterior) y su `toggleFollow()` nunca tuvo ese guard: llamaba directo a `following.togglePlayer(...)` sin verificar sesión ni pasar el `pushEndpoint`. Era la única página de las tres que dejaba seguir sin cuenta.

**Corrección en `src/pages/PlayerDetail.vue`:** replicado exactamente el patrón de `TeamDetail.vue`/`TournamentHome.vue` — se agregan `useAuthStore` y `usePWA`; `toggleFollow()` ahora primero revisa `auth.isLoggedIn` y redirige a `/login?redirect=<ruta actual>` si no hay sesión (sin tocar la API ni el estado local), y si hay sesión, solicita permiso de push si falta y pasa `pushEndpoint` a `following.togglePlayer(...)` (antes no se pasaba en absoluto).

**Sobre el cierre de sesión:** no se tocó nada del flujo de logout — el `auth.logout()` existente solo se dispara por acción explícita del usuario (botón "Cerrar sesión"), no hay temporizador ni interceptor que cierre sesión automáticamente en ningún punto de la app. Se deja como nota para futuras sesiones: cualquier cambio en el manejo de sesión/token debe preservar esto.

**Validado con:** en navegador, sin sesión iniciada (localStorage limpio) — clic en el corazón de la ficha de "Samuel Gutierrez" redirige correctamente a `/login?redirect=/COPACARIBE/jugador/134`, sin llamar a la API de follow ni cambiar el estado local. Build de producción limpio.
